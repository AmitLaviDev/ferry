---
phase: 24-test-repo-migration-e2e
plan: 01
type: execute
wave: 1
depends_on: [22-01, 23-01]
files_modified:
  - ferry-test-app/.github/workflows/ferry.yml (NEW)
  - ferry-test-app/.github/workflows/ferry-lambdas.yml (DELETE)
  - ferry-test-app/.github/workflows/ferry-step_functions.yml (DELETE)
  - ferry-test-app/.github/workflows/ferry-api_gateways.yml (DELETE)
autonomous: false
requirements:
  - VAL-01
  - VAL-02

must_haves:
  truths:
    - "ferry-test-app has exactly one workflow file: .github/workflows/ferry.yml"
    - "ferry-test-app has NO per-type workflow files (ferry-lambdas.yml, ferry-step_functions.yml, ferry-api_gateways.yml)"
    - "A Lambda source change triggers Ferry dispatch to ferry.yml and the deploy-lambda job succeeds"
    - "A Step Functions definition change triggers Ferry dispatch to ferry.yml and the deploy-step-function job succeeds"
    - "An API Gateway spec change triggers Ferry dispatch to ferry.yml and the deploy-api-gateway job succeeds"
  artifacts:
    - path: "ferry-test-app/.github/workflows/ferry.yml"
      provides: "Unified workflow with setup + 3 conditional deploy jobs"
      contains: "deploy-lambda"
  key_links:
    - from: "ferry-test-app/.github/workflows/ferry.yml"
      to: "AmitLaviDev/ferry/action/setup@main"
      via: "uses clause in setup job"
      pattern: "AmitLaviDev/ferry/action/setup@main"
    - from: "ferry backend Lambda"
      to: "ferry-test-app/.github/workflows/ferry.yml"
      via: "workflow_dispatch to ferry.yml filename"
      pattern: "WORKFLOW_FILENAME"
---

<objective>
Migrate the test repo to the unified workflow and validate all 3 resource types deploy successfully.

Purpose: Phase 22 changed the backend to dispatch to `ferry.yml`. Phase 23 created the template and documentation. This phase applies the template to the test repo, removes the old per-type workflow files, and proves end-to-end that Lambda, Step Functions, and API Gateway all deploy correctly through the single unified workflow.

Output: Test repo has one `ferry.yml` workflow, old files deleted, all 3 resource types validated E2E.
</objective>

<execution_context>
This phase operates on TWO repositories:
- Ferry monorepo: /Users/amit/Repos/github/ferry (backend code — already updated in Phase 22)
- Test repo: /Users/amit/Repos/github/ferry-test-app (workflow migration target)

All ferry-test-app paths are relative to /Users/amit/Repos/github/ferry-test-app.
The ferry.yml template source is docs/setup.md lines 60-186 in the ferry monorepo.
</execution_context>

<context>
@/Users/amit/Repos/github/ferry/iac/test-env/.planning/ROADMAP.md
@/Users/amit/Repos/github/ferry/iac/test-env/.planning/phases/22-backend-action-changes/22-01-SUMMARY.md
@/Users/amit/Repos/github/ferry/iac/test-env/.planning/phases/23-unified-workflow-template-and-docs/23-01-SUMMARY.md

<interfaces>
<!-- Phase 22 confirmed: backend dispatches to ferry.yml -->
From utils/src/ferry_utils/constants.py (Phase 22):
- WORKFLOW_FILENAME = "ferry.yml"
- All dispatch calls target this filename regardless of resource type

<!-- Phase 22 confirmed: setup action outputs -->
From action/setup/action.yml (Phase 22):
- Output: `matrix` (JSON string for strategy.matrix)
- Output: `resource_type` (string: 'lambda' | 'step_function' | 'api_gateway')

<!-- Test repo ferry.yaml configuration (unchanged) -->
From ferry-test-app/ferry.yaml:
```yaml
version: 1
lambdas:
  - name: hello-world
    source_dir: lambdas/hello-world
    ecr_repo: ferry-test/hello-world
    function_name: ferry-test-hello-world
    runtime: python3.12

step_functions:
  - name: hello-chain
    source_dir: workflows/hello-chain
    state_machine_name: ferry-test-sf
    definition_file: definition.json

api_gateways:
  - name: hello-chain
    source_dir: api/hello-chain
    rest_api_id: v1h1ch5rqk
    stage_name: test
    spec_file: openapi.yaml
```

<!-- AWS resources provisioned by iac/test-env Terraform -->
- Lambda: ferry-test-hello-world (us-east-1)
- ECR: ferry-test/hello-world (us-east-1)
- Step Function: ferry-test-sf (us-east-1)
- API Gateway: ferry-test-api, REST API ID v1h1ch5rqk, stage "test" (us-east-1)
- AWS Role: configured in ferry-test-app repo secret AWS_ROLE_ARN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified ferry.yml in test repo</name>
  <files>ferry-test-app/.github/workflows/ferry.yml</files>
  <action>
Create `.github/workflows/ferry.yml` in the test repo at `/Users/amit/Repos/github/ferry-test-app/.github/workflows/ferry.yml`.

Use the exact template from `docs/setup.md` (lines 60-186 of the ferry monorepo). The only customization needed: the template already uses `us-east-1` which matches the test environment.

The complete file content:

```yaml
# .github/workflows/ferry.yml
name: Ferry Deploy

run-name: "Ferry Deploy: ${{ github.event.inputs.payload && fromJson(github.event.inputs.payload).resource_type || 'manual' }}"

on:
  workflow_dispatch:
    inputs:
      payload:
        description: "Ferry dispatch payload (JSON) -- sent by Ferry App, not for manual use"
        required: true

env:
  AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
  AWS_REGION: us-east-1                    # Adjust to your AWS region

permissions:
  id-token: write
  contents: read
  checks: write

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.parse.outputs.matrix }}
      resource_type: ${{ steps.parse.outputs.resource_type }}
    steps:
      - uses: actions/checkout@v4
      - name: Parse Ferry payload
        id: parse
        uses: AmitLaviDev/ferry/action/setup@main
        with:
          payload: ${{ inputs.payload }}

  deploy-lambda:
    name: "Ferry: deploy ${{ matrix.name }}"
    needs: setup
    if: needs.setup.outputs.resource_type == 'lambda'
    runs-on: ubuntu-latest
    concurrency:
      group: ferry-deploy-lambda
      cancel-in-progress: false
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Build container
        id: build
        uses: AmitLaviDev/ferry/action/build@main
        with:
          resource-name: ${{ matrix.name }}
          source-dir: ${{ matrix.source }}
          ecr-repo: ${{ matrix.ecr }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          trigger-sha: ${{ matrix.trigger_sha }}
          deployment-tag: ${{ matrix.deployment_tag }}
          runtime: ${{ matrix.runtime }}
      - name: Deploy Lambda
        uses: AmitLaviDev/ferry/action/deploy@main
        with:
          resource-name: ${{ matrix.name }}
          function-name: ${{ matrix.function_name }}
          image-uri: ${{ steps.build.outputs.image-uri }}
          image-digest: ${{ steps.build.outputs.image-digest }}
          deployment-tag: ${{ matrix.deployment_tag }}
          trigger-sha: ${{ matrix.trigger_sha }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          github-token: ${{ github.token }}

  deploy-step-function:
    name: "Ferry: deploy ${{ matrix.name }}"
    needs: setup
    if: needs.setup.outputs.resource_type == 'step_function'
    runs-on: ubuntu-latest
    concurrency:
      group: ferry-deploy-step-function
      cancel-in-progress: false
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Step Functions
        uses: AmitLaviDev/ferry/action/deploy-stepfunctions@main
        with:
          resource-name: ${{ matrix.name }}
          state-machine-name: ${{ matrix.state_machine_name }}
          definition-file: ${{ matrix.definition_file }}
          source-dir: ${{ matrix.source }}
          trigger-sha: ${{ matrix.trigger_sha }}
          deployment-tag: ${{ matrix.deployment_tag }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          github-token: ${{ github.token }}

  deploy-api-gateway:
    name: "Ferry: deploy ${{ matrix.name }}"
    needs: setup
    if: needs.setup.outputs.resource_type == 'api_gateway'
    runs-on: ubuntu-latest
    concurrency:
      group: ferry-deploy-api-gateway
      cancel-in-progress: false
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Deploy API Gateway
        uses: AmitLaviDev/ferry/action/deploy-apigw@main
        with:
          resource-name: ${{ matrix.name }}
          rest-api-id: ${{ matrix.rest_api_id }}
          stage-name: ${{ matrix.stage_name }}
          spec-file: ${{ matrix.spec_file }}
          source-dir: ${{ matrix.source }}
          trigger-sha: ${{ matrix.trigger_sha }}
          deployment-tag: ${{ matrix.deployment_tag }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          github-token: ${{ github.token }}
```

Do NOT delete the old workflow files yet. They must coexist with `ferry.yml` temporarily.

Commit and push to test repo's main branch with message:
`feat: add unified ferry.yml workflow (v1.4 migration step 1)`
  </action>
  <verify>
    <automated>ls /Users/amit/Repos/github/ferry-test-app/.github/workflows/ferry.yml && echo "ferry.yml EXISTS" || echo "ferry.yml MISSING"</automated>
  </verify>
  <done>
    - ferry-test-app/.github/workflows/ferry.yml exists with full unified template
    - Template has: run-name with fromJson guard, env block, setup job with matrix + resource_type outputs, 3 conditional deploy jobs
    - Old per-type workflow files still present (will be deleted in Task 3)
    - Commit pushed to test repo main branch
  </done>
</task>

<task type="manual">
  <name>Task 2: Verify Ferry backend is deployed with Phase 22 changes</name>
  <files></files>
  <action>
The Ferry backend Lambda must be running the Phase 22 code that dispatches to `ferry.yml` (not per-type filenames). Verify this before proceeding.

**Check if the backend Lambda has the Phase 22 code deployed:**

```bash
# Check the deployed Lambda's code — look for WORKFLOW_FILENAME usage
aws lambda get-function --function-name ferry-app --region us-east-1 --query 'Configuration.LastModified' --output text --profile amit-private
```

If the backend was last deployed BEFORE the Phase 22 commit (2026-03-10), the backend needs redeployment:

```bash
# Redeploy the Ferry backend Lambda from the ferry monorepo main branch
cd /Users/amit/Repos/github/ferry
# Use whatever deployment method is established (manual zip upload, CI/CD, etc.)
```

**Verification:** After deployment, the backend should dispatch to `ferry.yml`. You can verify by:
1. Checking the Lambda code package contains `WORKFLOW_FILENAME = "ferry.yml"` in constants
2. OR triggering a test push and checking the GHA Actions tab for a `ferry.yml` dispatch

If the backend is already deployed with Phase 22 code, this task is a no-op — proceed to Task 3.
  </action>
  <verify>
    <manual>Backend Lambda is running Phase 22 code that dispatches to ferry.yml. Confirmed by either: (a) Lambda LastModified is after Phase 22 commit, or (b) a test dispatch targets ferry.yml.</manual>
  </verify>
  <done>
    - Ferry backend Lambda is deployed with WORKFLOW_FILENAME = "ferry.yml"
    - All dispatches now target ferry.yml in user repos
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete old per-type workflow files from test repo</name>
  <files>
    ferry-test-app/.github/workflows/ferry-lambdas.yml (DELETE)
    ferry-test-app/.github/workflows/ferry-step_functions.yml (DELETE)
    ferry-test-app/.github/workflows/ferry-api_gateways.yml (DELETE)
  </files>
  <action>
Delete the three old per-type workflow files from the test repo:

```bash
cd /Users/amit/Repos/github/ferry-test-app
git rm .github/workflows/ferry-lambdas.yml
git rm .github/workflows/ferry-step_functions.yml
git rm .github/workflows/ferry-api_gateways.yml
```

Commit and push with message:
`feat: remove per-type workflow files (v1.4 migration step 2)`

After this commit, the test repo should have exactly ONE workflow file: `.github/workflows/ferry.yml`.
  </action>
  <verify>
    <automated>ls /Users/amit/Repos/github/ferry-test-app/.github/workflows/ | sort</automated>
    <expected>ferry.yml</expected>
  </verify>
  <done>
    - ferry-lambdas.yml deleted from test repo
    - ferry-step_functions.yml deleted from test repo
    - ferry-api_gateways.yml deleted from test repo
    - Only ferry.yml remains in .github/workflows/
    - Commit pushed to test repo main branch
  </done>
</task>

<task type="manual">
  <name>Task 4: E2E validation — Lambda deploy via unified workflow</name>
  <files>ferry-test-app/lambdas/hello-world/</files>
  <action>
Trigger a Lambda deployment through the unified workflow by making a small change to the Lambda source.

**Step 1: Make a trivial change to Lambda source code:**

```bash
cd /Users/amit/Repos/github/ferry-test-app
# Add a comment or bump a version string in the Lambda handler
# e.g., edit lambdas/hello-world/handler.py — add a comment with today's date
```

**Step 2: Commit and push:**

```bash
git add lambdas/hello-world/
git commit -m "test: trigger Lambda deploy via unified workflow"
git push
```

**Step 3: Observe the Ferry dispatch chain:**

1. **Ferry App receives push webhook** — Check Ferry Lambda logs (CloudWatch) for the webhook processing
2. **Ferry detects Lambda source change** — The `ferry.yaml` maps `lambdas/hello-world/` as a Lambda resource
3. **Ferry dispatches to `ferry.yml`** — Check GHA Actions tab for a new "Ferry Deploy: lambda" run
4. **Setup job parses payload** — Should output `resource_type=lambda` and `matrix` with hello-world entry
5. **deploy-lambda job runs** — Should be the ONLY deploy job that runs; deploy-step-function and deploy-api-gateway should be skipped
6. **Build + Deploy succeed** — Container built, pushed to ECR, Lambda function updated

**Verification checkpoints:**
- [ ] GHA Actions tab shows run with name "Ferry Deploy: lambda"
- [ ] deploy-lambda job ran and succeeded
- [ ] deploy-step-function and deploy-api-gateway jobs are skipped (grey)
- [ ] Lambda function `ferry-test-hello-world` was updated (check AWS Console or `aws lambda get-function`)
  </action>
  <verify>
    <manual>GHA Actions tab shows "Ferry Deploy: lambda" run with deploy-lambda job succeeded, other deploy jobs skipped, and Lambda function updated in AWS.</manual>
  </verify>
  <done>
    - Lambda deploy triggered via unified ferry.yml workflow
    - deploy-lambda job ran and succeeded
    - deploy-step-function and deploy-api-gateway jobs were skipped
    - Lambda function ferry-test-hello-world updated in AWS
  </done>
</task>

<task type="manual">
  <name>Task 5: E2E validation — Step Functions deploy via unified workflow</name>
  <files>ferry-test-app/workflows/hello-chain/</files>
  <action>
Trigger a Step Functions deployment through the unified workflow.

**Step 1: Make a trivial change to the Step Functions definition:**

```bash
cd /Users/amit/Repos/github/ferry-test-app
# Edit workflows/hello-chain/definition.json — add/modify a Comment field or similar
```

**Step 2: Commit and push:**

```bash
git add workflows/hello-chain/
git commit -m "test: trigger Step Functions deploy via unified workflow"
git push
```

**Step 3: Observe the Ferry dispatch chain:**

1. **Ferry detects definition change** — `ferry.yaml` maps `workflows/hello-chain/` as a Step Function resource
2. **Ferry dispatches to `ferry.yml`** with `resource_type=step_function`
3. **GHA Actions tab** shows "Ferry Deploy: step_function" run
4. **deploy-step-function job runs** — deploy-lambda and deploy-api-gateway should be skipped
5. **State machine updated** in AWS

**Verification checkpoints:**
- [ ] GHA Actions tab shows run with name "Ferry Deploy: step_function"
- [ ] deploy-step-function job ran and succeeded
- [ ] deploy-lambda and deploy-api-gateway jobs are skipped
- [ ] State machine `ferry-test-sf` was updated (check AWS Console or `aws stepfunctions describe-state-machine`)
  </action>
  <verify>
    <manual>GHA Actions tab shows "Ferry Deploy: step_function" run with deploy-step-function job succeeded, other deploy jobs skipped, and state machine updated in AWS.</manual>
  </verify>
  <done>
    - Step Functions deploy triggered via unified ferry.yml workflow
    - deploy-step-function job ran and succeeded
    - deploy-lambda and deploy-api-gateway jobs were skipped
    - State machine ferry-test-sf updated in AWS
  </done>
</task>

<task type="manual">
  <name>Task 6: E2E validation — API Gateway deploy via unified workflow</name>
  <files>ferry-test-app/api/hello-chain/</files>
  <action>
Trigger an API Gateway deployment through the unified workflow.

**Step 1: Make a trivial change to the API Gateway spec:**

```bash
cd /Users/amit/Repos/github/ferry-test-app
# Edit api/hello-chain/openapi.yaml — modify a description field or add a comment
```

**Step 2: Commit and push:**

```bash
git add api/hello-chain/
git commit -m "test: trigger API Gateway deploy via unified workflow"
git push
```

**Step 3: Observe the Ferry dispatch chain:**

1. **Ferry detects spec change** — `ferry.yaml` maps `api/hello-chain/` as an API Gateway resource
2. **Ferry dispatches to `ferry.yml`** with `resource_type=api_gateway`
3. **GHA Actions tab** shows "Ferry Deploy: api_gateway" run
4. **deploy-api-gateway job runs** — deploy-lambda and deploy-step-function should be skipped
5. **REST API updated** in AWS

**Verification checkpoints:**
- [ ] GHA Actions tab shows run with name "Ferry Deploy: api_gateway"
- [ ] deploy-api-gateway job ran and succeeded
- [ ] deploy-lambda and deploy-step-function jobs are skipped
- [ ] REST API `v1h1ch5rqk` stage `test` was updated (check AWS Console or `aws apigateway get-rest-api`)
  </action>
  <verify>
    <manual>GHA Actions tab shows "Ferry Deploy: api_gateway" run with deploy-api-gateway job succeeded, other deploy jobs skipped, and REST API updated in AWS.</manual>
  </verify>
  <done>
    - API Gateway deploy triggered via unified ferry.yml workflow
    - deploy-api-gateway job ran and succeeded
    - deploy-lambda and deploy-step-function jobs were skipped
    - REST API v1h1ch5rqk updated in AWS
  </done>
</task>

</tasks>

<verification>
After all 6 tasks complete, verify the full phase success criteria:

1. **Single workflow file:**
   ```bash
   ls /Users/amit/Repos/github/ferry-test-app/.github/workflows/
   # Expected: ferry.yml (only)
   ```

2. **No old workflow files:**
   ```bash
   ls /Users/amit/Repos/github/ferry-test-app/.github/workflows/ferry-*.yml 2>&1
   # Expected: No such file or directory
   ```

3. **All 3 resource types validated:**
   - Lambda: "Ferry Deploy: lambda" run succeeded in GHA Actions
   - Step Functions: "Ferry Deploy: step_function" run succeeded in GHA Actions
   - API Gateway: "Ferry Deploy: api_gateway" run succeeded in GHA Actions

4. **Job routing correct for all 3 runs:**
   - Each run has exactly ONE deploy job that ran (green)
   - The other TWO deploy jobs are skipped (grey) in each run
</verification>

<success_criteria>
1. `AmitLaviDev/ferry-test-app` has exactly one workflow file: `.github/workflows/ferry.yml`
2. The three old workflow files (`ferry-lambdas.yml`, `ferry-step_functions.yml`, `ferry-api_gateways.yml`) are deleted
3. A Lambda source push triggered Ferry dispatch → deploy-lambda job succeeded → Lambda function updated
4. A Step Functions definition push triggered Ferry dispatch → deploy-step-function job succeeded → state machine updated
5. An API Gateway spec push triggered Ferry dispatch → deploy-api-gateway job succeeded → REST API updated
</success_criteria>

<output>
After completion, create `.planning/phases/24-test-repo-migration-e2e/24-01-SUMMARY.md` with:
- Migration steps completed (ferry.yml added, old files deleted)
- E2E validation results for each resource type (GHA run URLs if available)
- Requirements satisfied (VAL-01, VAL-02)
- Any deviations from plan or issues encountered

No commit in the ferry monorepo needed — all changes are in the test repo.
Update `.planning/STATE.md` and `.planning/ROADMAP.md` to mark Phase 24 and v1.4 milestone as complete.
</output>
