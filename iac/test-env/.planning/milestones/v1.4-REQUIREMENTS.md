# v1.4 Requirements (Archived)

**Milestone:** v1.4 Unified Workflow
**Status:** All requirements satisfied
**Archived:** 2026-03-10

## Backend

- [x] **BE-01**: All dispatches target `ferry.yml` regardless of resource type
- [x] **BE-02**: `RESOURCE_TYPE_WORKFLOW_MAP` removed, single `WORKFLOW_FILENAME` constant exists

## Action

- [x] **ACT-01**: Setup action exposes `resource_type` as a workflow output
- [x] **ACT-02**: Existing `matrix` output preserved (no regression)

## Workflow Template

- [x] **WF-01**: `ferry.yml` has setup job + 3 conditional deploy jobs
- [x] **WF-02**: Lambda deploy job uses matrix strategy
- [x] **WF-03**: Step Functions deploy job uses matrix strategy with `fail-fast: false`
- [x] **WF-04**: API Gateway deploy job uses matrix strategy with `fail-fast: false`
- [x] **WF-05**: `run-name` displays resource type in GHA Actions UI
- [x] **WF-06**: No workflow-level concurrency; job-level concurrency groups keyed by type

## Documentation

- [x] **DOC-01**: setup.md has unified template + migration guide
- [x] **DOC-02**: Per-type pages have workflow sections removed

## Validation

- [x] **VAL-01**: Test repo migrated to single `ferry.yml`, old files deleted
- [x] **VAL-02**: All 3 resource types deploy successfully via unified workflow

## Outcomes

All 14 requirements delivered as specified. No adjustments or drops.
