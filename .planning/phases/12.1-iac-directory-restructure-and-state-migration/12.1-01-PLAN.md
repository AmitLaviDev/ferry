---
phase: 12.1-iac-directory-restructure-and-state-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iac/global/aws/backend/providers.tf
  - iac/global/aws/ecr/providers.tf
  - iac/global/aws/oidc/providers.tf
  - iac/staging/aws/shared/providers.tf
  - iac/staging/aws/shared/data.tf
  - scripts/migrate-iac-layout.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 4 providers.tf files have updated S3 backend keys matching the new directory layout"
    - "Shared project data.tf references OIDC remote state at the new S3 key"
    - "Migration script exists and handles all 4 project moves in correct order (backend, ecr, oidc, shared)"
    - "Migration script validates each project with terraform plan after init -migrate-state"
  artifacts:
    - path: "iac/global/aws/backend/providers.tf"
      provides: "Updated backend key: global/cloud/aws/backend/terraform.tfstate"
      contains: "global/cloud/aws/backend/terraform.tfstate"
    - path: "iac/global/aws/ecr/providers.tf"
      provides: "Updated backend key: global/cloud/aws/ecr/terraform.tfstate"
      contains: "global/cloud/aws/ecr/terraform.tfstate"
    - path: "iac/global/aws/oidc/providers.tf"
      provides: "Updated backend key: global/cloud/aws/oidc/terraform.tfstate"
      contains: "global/cloud/aws/oidc/terraform.tfstate"
    - path: "iac/staging/aws/shared/providers.tf"
      provides: "Updated backend key: aws/staging/shared/terraform.tfstate"
      contains: "aws/staging/shared/terraform.tfstate"
    - path: "iac/staging/aws/shared/data.tf"
      provides: "Updated OIDC remote state key reference"
      contains: "global/cloud/aws/oidc/terraform.tfstate"
    - path: "scripts/migrate-iac-layout.sh"
      provides: "Executable migration script for directory moves and state migration"
      min_lines: 60
  key_links:
    - from: "scripts/migrate-iac-layout.sh"
      to: "iac/*/providers.tf"
      via: "Script expects providers.tf to already have new backend keys before running terraform init -migrate-state"
      pattern: "terraform.*init.*-migrate-state"
    - from: "iac/staging/aws/shared/data.tf"
      to: "iac/global/aws/oidc/providers.tf"
      via: "terraform_remote_state.oidc references OIDC project's state key"
      pattern: "global/cloud/aws/oidc/terraform.tfstate"
---

<objective>
Update all Terraform backend keys and remote state references to match the target directory layout, and create a migration script that handles the directory moves and state migration.

Purpose: Prepare the codebase for the IaC directory restructure. Code changes (new backend keys) are committed first, then the user runs the migration script which performs git mv + terraform init -migrate-state + terraform plan validation for each project.

Output: 5 updated .tf files with new S3 backend keys, 1 executable migration script at scripts/migrate-iac-layout.sh
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-iac-directory-restructure-and-state-migration/12.1-CONTEXT.md
@.planning/phases/12.1-iac-directory-restructure-and-state-migration/12.1-RESEARCH.md

Key file references:
@iac/global/aws/backend/providers.tf — current key: global/aws/backend/terraform.tfstate
@iac/global/aws/ecr/providers.tf — current key: global/aws/ecr/terraform.tfstate
@iac/global/aws/oidc/providers.tf — current key: global/aws/oidc/terraform.tfstate
@iac/staging/aws/shared/providers.tf — current key: staging/aws/shared/terraform.tfstate
@iac/staging/aws/shared/data.tf — has terraform_remote_state.oidc with old key
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Terraform backend keys and remote state references</name>
  <files>
    iac/global/aws/backend/providers.tf
    iac/global/aws/ecr/providers.tf
    iac/global/aws/oidc/providers.tf
    iac/staging/aws/shared/providers.tf
    iac/staging/aws/shared/data.tf
  </files>
  <action>
    Update the S3 backend `key` in each providers.tf to match the new directory layout. Update the remote state reference in shared/data.tf.

    Exact changes (5 files, 5 line edits total):

    1. `iac/global/aws/backend/providers.tf` line 8:
       - Old: `key = "global/aws/backend/terraform.tfstate"`
       - New: `key = "global/cloud/aws/backend/terraform.tfstate"`

    2. `iac/global/aws/ecr/providers.tf` line 6:
       - Old: `key = "global/aws/ecr/terraform.tfstate"`
       - New: `key = "global/cloud/aws/ecr/terraform.tfstate"`

    3. `iac/global/aws/oidc/providers.tf` line 6:
       - Old: `key = "global/aws/oidc/terraform.tfstate"`
       - New: `key = "global/cloud/aws/oidc/terraform.tfstate"`

    4. `iac/staging/aws/shared/providers.tf` line 6:
       - Old: `key = "staging/aws/shared/terraform.tfstate"`
       - New: `key = "aws/staging/shared/terraform.tfstate"`

    5. `iac/staging/aws/shared/data.tf` line 14 (inside terraform_remote_state.oidc):
       - Old: `key = "global/aws/oidc/terraform.tfstate"`
       - New: `key = "global/cloud/aws/oidc/terraform.tfstate"`

    No other files need content changes. All other .tf files in each project (main.tf, variables.tf, outputs.tf, iam.tf, secrets.tf) move unchanged with their directories.
  </action>
  <verify>
    <automated>grep -r 'key.*=.*"global/aws/' iac/ --include='*.tf' | grep -v '.terraform/' | grep -v 'global/cloud/aws' ; test $? -eq 1 && grep -r 'key.*=.*"staging/aws/' iac/ --include='*.tf' | grep -v '.terraform/' ; test $? -eq 1 && echo "All keys updated correctly"</automated>
    <manual>Verify no providers.tf or data.tf file still references old S3 keys (global/aws/ without cloud/ or staging/aws/)</manual>
  </verify>
  <done>All 4 providers.tf files have new backend keys matching target directory layout. Shared data.tf references OIDC at new key. No .tf file (outside .terraform/) references old S3 keys.</done>
</task>

<task type="auto">
  <name>Task 2: Create migration script</name>
  <files>
    scripts/migrate-iac-layout.sh
  </files>
  <action>
    Create `scripts/migrate-iac-layout.sh` — an executable shell script that performs the directory restructure and state migration. The script assumes the code changes from Task 1 are already committed.

    Script requirements (from CONTEXT.md locked decisions):
    - `set -euo pipefail` for fail-fast
    - Pre-flight checks: verify `terraform` is available, verify source directories exist
    - Create target parent directories with `mkdir -p` before `git mv` (Pitfall 3 from RESEARCH.md)
    - Process 4 projects in order: backend, ecr, oidc, shared (OIDC must complete before shared)
    - For each project:
      1. Delete `.terraform/` directory from source location BEFORE git mv (clean slate per CONTEXT.md)
      2. `git mv` source to target directory
      3. `terraform -chdir=<new_path> init -migrate-state -force-copy -input=false`
      4. `terraform -chdir=<new_path> plan -input=false` to validate "No changes"
    - After all migrations, clean up empty directories (`rmdir` for iac/global/aws/, iac/staging/aws/, iac/staging/)
    - Clear progress indicators: `[1/4] Migrating backend...` etc.
    - Print summary at end

    Directory moves:
    - `iac/global/aws/backend` -> `iac/global/cloud/aws/backend`
    - `iac/global/aws/ecr` -> `iac/global/cloud/aws/ecr`
    - `iac/global/aws/oidc` -> `iac/global/cloud/aws/oidc`
    - `iac/staging/aws/shared` -> `iac/aws/staging/shared`

    Claude's discretion items (from CONTEXT.md):
    - No explicit state backup step — S3 versioning + copy-not-move behavior provides natural backup (per RESEARCH.md recommendation)
    - No .terraform.lock.hcl special handling — they are gitignored and regenerated by `terraform init` (per RESEARCH.md Pitfall 5)
    - Script uses `REPO_ROOT` computed from script location for portability
    - Use a `migrate_project` helper function for DRY per-project logic

    Make the script executable: `chmod +x scripts/migrate-iac-layout.sh`
  </action>
  <verify>
    <automated>bash -n scripts/migrate-iac-layout.sh && test -x scripts/migrate-iac-layout.sh && echo "Script is valid bash and executable"</automated>
    <manual>Read the script and verify: (1) correct order (backend, ecr, oidc, shared), (2) .terraform/ deleted before git mv, (3) -migrate-state -force-copy -input=false flags, (4) terraform plan validation after each migration</manual>
  </verify>
  <done>scripts/migrate-iac-layout.sh exists, is executable, passes bash -n syntax check, handles all 4 project migrations in correct order with fail-fast error handling.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'key.*=.*"global/aws/' iac/ --include='*.tf' | grep -v '.terraform/'` returns no results (old global keys gone)
2. `grep -rn 'key.*=.*"staging/aws/' iac/ --include='*.tf' | grep -v '.terraform/'` returns no results (old staging keys gone)
3. `grep -rn 'global/cloud/aws/backend/terraform.tfstate' iac/global/aws/backend/providers.tf` finds the new key
4. `grep -rn 'global/cloud/aws/oidc/terraform.tfstate' iac/staging/aws/shared/data.tf` finds the updated remote state reference
5. `bash -n scripts/migrate-iac-layout.sh` passes (valid bash syntax)
6. `test -x scripts/migrate-iac-layout.sh` passes (executable)
</verification>

<success_criteria>
- All 5 .tf files updated with correct new S3 backend keys
- Migration script created, valid syntax, executable
- Migration script processes projects in correct order: backend -> ecr -> oidc -> shared
- No .tf file outside .terraform/ references old S3 keys (global/aws/ without cloud/ prefix, or staging/aws/)
</success_criteria>

<manual_steps>
After this plan completes and code is committed, the user must:
1. Run `scripts/migrate-iac-layout.sh` from the repo root (requires AWS credentials configured locally)
2. Verify each `terraform plan` shows "No changes" (the script does this automatically but user should watch output)
3. Run `git status` to see the moved files, then `git add -A && git commit` the directory restructure
</manual_steps>

<output>
After completion, create `.planning/phases/12.1-iac-directory-restructure-and-state-migration/12.1-01-SUMMARY.md`
</output>
