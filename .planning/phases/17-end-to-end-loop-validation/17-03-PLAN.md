---
phase: 17-end-to-end-loop-validation
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - .planning/phases/17-end-to-end-loop-validation/17-VALIDATION-REPORT.md
autonomous: false
requirements: [E2E-09, E2E-08]

must_haves:
  truths:
    - "Second push with dependency change triggers successful deploy"
    - "No-op push (non-Lambda file) is received but skips dispatch"
    - "Validation report documents all steps, bugs, and proof"
  artifacts:
    - path: ".planning/phases/17-end-to-end-loop-validation/17-VALIDATION-REPORT.md"
      provides: "Complete validation report with proof links"
      contains: "Repeatability"
  key_links:
    - from: "requirements.txt change"
      to: "full rebuild + deploy"
      via: "Ferry change detection matching source_dir"
      pattern: "changes_detected in CloudWatch"
    - from: "README change"
      to: "no dispatch"
      via: "Ferry change detection finding no matching resources"
      pattern: "webhook_event_received but no dispatches_triggered"
---

<objective>
Prove the push-to-deploy loop is repeatable with a second push (dependency change) and a no-op push (non-Lambda file). Write the validation report documenting the full E2E proof.

Purpose: E2E-09 requires a second successful push to prove repeatability. The no-op test proves Ferry correctly skips dispatch when no resources are affected. The validation report is the completion artifact for v1.2.
Output: Repeatability proven, validation report written.
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-end-to-end-loop-validation/17-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Second push (dependency change) and no-op test</name>
  <action>
First push succeeded in Plan 02. Now prove repeatability with a different kind of change, and test the negative case.

**Part A: Second push -- dependency/config change**

```bash
cd /path/to/ferry-test-app
# Add a harmless dependency to requirements.txt
echo "requests>=2.31.0" >> lambdas/hello-world/requirements.txt
git add lambdas/hello-world/requirements.txt
git commit -m "chore: add requests dependency"
git push origin main
```

Follow the same verification steps as Plan 02 Task 2 (Steps B-F):
- B: Check CloudWatch for webhook processing
- C: Verify workflow_dispatch triggered (`gh run list -R AmitLaviDev/ferry-test-app --limit 3`)
- D: Wait for GHA workflow to complete
- E: Verify Lambda LastModified updated
- F: Invoke Lambda -- should still return v2 greeting (code unchanged, just deps rebuilt)

**Part B: No-op push -- non-Lambda file change**

```bash
cd /path/to/ferry-test-app
echo "" >> README.md
echo "Updated $(date)" >> README.md
git add README.md
git commit -m "docs: update README"
git push origin main
```

Verify:
- CloudWatch shows webhook processing for this push
- CloudWatch shows `changes_detected` with `affected_count=0`
- NO new workflow_dispatch triggered (`gh run list -R AmitLaviDev/ferry-test-app --limit 3` should show no new run)
  </action>
  <verify>Part A: second push triggers full loop and Lambda invocation succeeds. Part B: no-op push received but no dispatch triggered.</verify>
  <done>Two successful push-to-deploy cycles proven (code + deps). No-op push correctly skipped. E2E-09 satisfied.</done>
  <resume-signal>Report Part A (second push) and Part B (no-op) results. "Both passed" or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write validation report</name>
  <files>.planning/phases/17-end-to-end-loop-validation/17-VALIDATION-REPORT.md</files>
  <action>
Create the validation report at `.planning/phases/17-end-to-end-loop-validation/17-VALIDATION-REPORT.md`. Use information gathered from the checkpoint tasks in Plans 02 and 03 (available in SUMMARY files).

Structure:

```markdown
# Ferry v1.2 End-to-End Validation Report

**Date:** [date]
**Status:** [PASSED/FAILED]

## Infrastructure Verified

| Component | Status | Evidence |
|-----------|--------|----------|
| Ferry Lambda | [status] | [Function URL, CloudWatch log group] |
| DynamoDB dedup table | [status] | [table name] |
| GitHub App | [status] | [app name, installation ID] |
| Self-deploy pipeline | [status] | [GHA run URL] |
| Test ECR repo | [status] | [repo URI] |
| Test Lambda | [status] | [function name, ARN] |
| Test OIDC role | [status] | [role ARN] |

## E2E Loop Results

### Push 1: Code Change (main.py)
- Webhook received: [yes/no, CloudWatch evidence]
- Changes detected: [count, resources]
- Dispatch triggered: [yes/no, GHA run URL]
- Build completed: [yes/no, duration]
- Deploy completed: [yes/no]
- Lambda invocation: [response body]

### Push 2: Dependency Change (requirements.txt)
[same structure]

### No-op Push: README Change
- Webhook received: [yes/no]
- Changes detected: 0
- Dispatch triggered: no (correct behavior)

## Bugs Found and Fixed

| Bug | Symptom | Fix | Commit |
|-----|---------|-----|--------|
| find_open_prs 403 crash | [symptom] | [fix] | [hash] |
| [any others] | ... | ... | ... |

## Known Limitations

What v1.2 proved:
- Single Lambda push-to-deploy on default branch (main)
- Magic Dockerfile build for Python runtime
- OIDC authentication for GHA runners

What v1.2 did NOT prove (future work):
- PR event handling (Check Runs, PR comments) -- v2
- Step Functions / API Gateway deploy -- v1.3
- Multi-resource ferry.yaml (only tested single Lambda)
- Multi-tenant / cross-org installations -- v2
- Rollback scenarios

## Resource Links

- Ferry Lambda CloudWatch: [log group ARN or link]
- Ferry Function URL: [URL]
- GitHub App: [settings URL]
- Test repo: https://github.com/AmitLaviDev/ferry-test-app
- Test Lambda: [ARN]
- GHA workflow runs: [URL to test repo Actions tab]
```

Fill in all placeholders with actual values from the Plan 02 and Plan 03 SUMMARY files. If specific values are not available in SUMMARYs, use descriptive placeholders that the user can fill in.

Per user decision: do NOT close the v1.2 milestone.
  </action>
  <verify>
    <automated>test -f /Users/amit/Repos/github/ferry/.planning/phases/17-end-to-end-loop-validation/17-VALIDATION-REPORT.md && echo "Report exists"</automated>
    <manual>Report contains all sections: Infrastructure, E2E Loop Results (2 pushes + no-op), Bugs, Known Limitations, Resource Links</manual>
  </verify>
  <done>Validation report exists with all sections filled, documenting the complete E2E proof including bugs found/fixed, invocation results, and known limitations</done>
</task>

</tasks>

<verification>
- Second push triggers successful deploy (different change type than first)
- No-op push correctly skips dispatch
- Validation report captures full proof with resource links
</verification>

<success_criteria>
- Two successful push-to-deploy cycles proven (code change + dependency change)
- No-op test confirms correct skip behavior
- Validation report documents everything: steps, bugs, proof, limitations
- All E2E requirements (E2E-01 through E2E-09) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/17-end-to-end-loop-validation/17-03-SUMMARY.md`
</output>
