---
phase: 16-provision-test-environment
plan: 01
subsystem: infra
tags: [terraform, ecr, iam, oidc, lambda, aws]

# Dependency graph
requires:
  - phase: 11-iac-bootstrap
    provides: "S3 state bucket, OIDC provider"
  - phase: 12-shared-iam
    provides: "IAM patterns, policy document conventions"
provides:
  - "ECR repository for test Lambda images (ferry-test/hello-world)"
  - "IAM deploy role with OIDC trust for AmitLaviDev/ferry-test-app"
  - "Pre-created Lambda function (ferry-test-hello-world)"
  - "Outputs: role ARN, ECR URL, Lambda name/ARN"
affects: [16-02-PLAN, 16-03-PLAN, 17-e2e-validation]

# Tech tracking
tech-stack:
  added: [terraform-aws-modules/ecr/aws@2.4.0, terraform-aws-modules/lambda/aws@8.7.0]
  patterns: [test-env-terraform-project, oidc-trust-per-repo]

key-files:
  created:
    - iac/test-env/providers.tf
    - iac/test-env/variables.tf
    - iac/test-env/data.tf
    - iac/test-env/main.tf
    - iac/test-env/outputs.tf
    - iac/test-env/README.md
  modified: []

key-decisions:
  - "Combined Tasks 1 and 2 into single commit due to tflint requiring all declarations to be used"
  - "Lambda IAM policy includes PublishVersion, UpdateAlias, CreateAlias, GetAlias to match deploy.py API calls"
  - "Used ignore_source_code_hash=true (module built-in) instead of lifecycle ignore_changes on image_uri"

patterns-established:
  - "test-env Terraform project: self-contained project with OIDC trust scoped to specific test repo"

requirements-completed: [TEST-04, TEST-05]

# Metrics
duration: 2min
completed: 2026-03-07
---

# Phase 16 Plan 01: Test-Env Terraform Project Summary

**Terraform project with ECR repo, OIDC-trusted IAM role, and pre-created Lambda for E2E test environment**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-07T12:57:36Z
- **Completed:** 2026-03-07T13:00:12Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Complete Terraform project at `iac/test-env/` ready for `terraform init && terraform apply`
- ECR repository (`ferry-test/hello-world`) with lifecycle policy keeping last 10 images
- IAM deploy role with OIDC trust scoped to `AmitLaviDev/ferry-test-app` with ECR push + Lambda deploy permissions
- Pre-created Lambda function (`ferry-test-hello-world`) with placeholder image and execution role

## Task Commits

Each task was committed atomically:

1. **Task 1+2: Terraform project skeleton + resources and outputs** - `1eb3078` (feat)
   - Combined because tflint requires all declarations to be used -- data sources in data.tf must be referenced by main.tf

**Plan metadata:** (pending)

## Files Created/Modified
- `iac/test-env/providers.tf` - S3 backend config, AWS provider with ferry-test default tags
- `iac/test-env/variables.tf` - Configurable variables for repo, region, ECR, Lambda
- `iac/test-env/data.tf` - OIDC remote state, trust policy, ECR/Lambda permission policy documents
- `iac/test-env/main.tf` - ECR module, IAM role+3 policies, Lambda module, execution role
- `iac/test-env/outputs.tf` - Role ARN, ECR URL, Lambda function name and ARN
- `iac/test-env/README.md` - Auto-generated by terraform-docs

## Decisions Made
- Combined Tasks 1 and 2 into a single commit because tflint enforces `terraform_unused_declarations` -- data sources and variables must be referenced by resources in the same commit
- Lambda deploy IAM policy includes all 6 actions from deploy.py: UpdateFunctionCode, GetFunction, PublishVersion, UpdateAlias, CreateAlias, GetAlias
- Used `ignore_source_code_hash = true` on the Lambda module (built-in flag) rather than attempting a lifecycle block on the module call

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added test_lambda_assume_role policy document to data.tf**
- **Found during:** Task 2 (Lambda execution role)
- **Issue:** Plan specified adding Lambda assume role trust policy in data.tf but listed it under Task 2 action
- **Fix:** Added `data "aws_iam_policy_document" "test_lambda_assume_role"` in data.tf during Task 1 creation
- **Files modified:** iac/test-env/data.tf
- **Verification:** tflint passed, all references resolve
- **Committed in:** 1eb3078

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to satisfy tflint unused declarations rule. No scope creep.

## Issues Encountered
- tflint `terraform_unused_declarations` rule requires all data sources and variables to be referenced, preventing atomic Task 1 commit (providers/variables/data only). Resolved by committing all TF files together.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Terraform project is ready for `terraform init && terraform apply`
- Requires `lambda_placeholder_image_uri` variable to be set (e.g., `public.ecr.aws/lambda/python:3.12`)
- Outputs provide role ARN and ECR URL needed for test repo secrets (Plan 02/03)

## Self-Check: PASSED

All 6 created files verified on disk. Commit 1eb3078 verified in git log. SUMMARY.md exists.

---
*Phase: 16-provision-test-environment*
*Completed: 2026-03-07*
