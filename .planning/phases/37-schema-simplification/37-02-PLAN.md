---
phase: 37-schema-simplification
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - action/src/ferry_action/parse_payload.py
  - action/src/ferry_action/deploy.py
  - action/src/ferry_action/deploy_stepfunctions.py
  - action/deploy/action.yml
  - action/deploy-stepfunctions/action.yml
  - docs/setup.md
  - tests/test_action/test_parse_payload.py
  - tests/test_action/test_deploy.py
  - tests/test_action/test_deploy_stepfunctions.py
autonomous: false
requirements: [SCHEMA-01]

must_haves:
  truths:
    - "parse_payload matrix entries use name (not function_name/state_machine_name) for AWS resource name"
    - "deploy.py reads INPUT_RESOURCE_NAME as the Lambda function name (no INPUT_FUNCTION_NAME)"
    - "deploy_stepfunctions.py reads INPUT_RESOURCE_NAME as the state machine name (no INPUT_STATE_MACHINE_NAME)"
    - "Composite action.yml files pass resource-name only (no function-name/state-machine-name inputs)"
    - "Workflow template in docs/setup.md uses matrix.name (no matrix.function_name/state_machine_name)"
    - "ferry.yaml docs show name as the AWS resource name"
    - "ferry-test-app ferry.yaml migrated to use name as AWS resource name"
  artifacts:
    - path: "action/src/ferry_action/parse_payload.py"
      provides: "Matrix builders without function_name/state_machine_name keys"
    - path: "action/src/ferry_action/deploy.py"
      provides: "Lambda deploy using INPUT_RESOURCE_NAME as function name"
    - path: "action/src/ferry_action/deploy_stepfunctions.py"
      provides: "SF deploy using INPUT_RESOURCE_NAME as state machine name"
    - path: "action/deploy/action.yml"
      provides: "Composite action without function-name input"
    - path: "action/deploy-stepfunctions/action.yml"
      provides: "Composite action without state-machine-name input"
    - path: "docs/setup.md"
      provides: "Updated workflow template and ferry.yaml docs"
  key_links:
    - from: "action/src/ferry_action/parse_payload.py"
      to: "docs/setup.md"
      via: "Matrix output keys match workflow template references"
      pattern: "matrix\\.name"
    - from: "action/deploy/action.yml"
      to: "action/src/ferry_action/deploy.py"
      via: "INPUT_RESOURCE_NAME env var"
      pattern: "INPUT_RESOURCE_NAME"
    - from: "action/deploy-stepfunctions/action.yml"
      to: "action/src/ferry_action/deploy_stepfunctions.py"
      via: "INPUT_RESOURCE_NAME env var"
      pattern: "INPUT_RESOURCE_NAME"
---

<objective>
Update the ferry-action layer to use `name` as the AWS resource name throughout: matrix output, deploy scripts, composite actions, workflow template, and docs.

Purpose: Complete the schema simplification by propagating the model changes from Plan 01 through the GHA action layer. After this plan, the entire pipeline from ferry.yaml -> dispatch -> matrix -> deploy uses `name` consistently.

Output: Updated action code, composite actions, docs, all action tests passing, ferry-test-app migrated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-schema-simplification/37-RESEARCH.md
@.planning/phases/37-schema-simplification/37-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, these models are simplified: -->

From utils/src/ferry_utils/models/dispatch.py (AFTER Plan 01):
```python
class LambdaResource(BaseModel):
    resource_type: Literal["lambda"] = "lambda"
    name: str       # IS the AWS function name
    source: str
    ecr: str
    runtime: str

class StepFunctionResource(BaseModel):
    resource_type: Literal["step_function"] = "step_function"
    name: str       # IS the AWS state machine name
    source: str
    definition_file: str
```

From action/deploy/action.yml (CURRENT -- to be modified):
```yaml
inputs:
  resource-name:       # KEEP -- becomes the function name
  function-name:       # REMOVE
  image-uri:           # KEEP
  image-digest:        # KEEP
  deployment-tag:      # KEEP
```

From action/deploy-stepfunctions/action.yml (CURRENT -- to be modified):
```yaml
inputs:
  resource-name:       # KEEP -- becomes the state machine name
  state-machine-name:  # REMOVE
  definition-file:     # KEEP
  source-dir:          # KEEP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parse_payload matrix builders and deploy scripts</name>
  <files>
    action/src/ferry_action/parse_payload.py,
    action/src/ferry_action/deploy.py,
    action/src/ferry_action/deploy_stepfunctions.py
  </files>
  <action>
**action/src/ferry_action/parse_payload.py:**

1. In `_build_lambda_matrix()`: Remove `"function_name": r.function_name` from the matrix entry dict. The entry already has `"name": r.name` which IS the function name now.

2. In `_build_step_function_matrix()`: Remove `"state_machine_name": r.state_machine_name` from the matrix entry dict. The entry already has `"name": r.name` which IS the state machine name now.

3. In `_parse_v2()`:
   - In `lambda_entries` list comprehension: Remove `"function_name": r.function_name`. The entry already has `"name": r.name`.
   - In `sf_entries` list comprehension: Remove `"state_machine_name": r.state_machine_name`. The entry already has `"name": r.name`.

4. Update the `build_matrix()` docstring: Remove `function_name` from the lambda entry field list, remove `state_machine_name` from the step_function entry field list.

**action/src/ferry_action/deploy.py:**

5. In `main()`:
   - Remove the `function_name = os.environ.get("INPUT_FUNCTION_NAME", "")` line and the if-not-function_name error block (lines 162-168).
   - Instead, set `function_name = os.environ["INPUT_RESOURCE_NAME"]` (the resource name IS the function name). Keep `resource_name` as well for display/logging: `resource_name = os.environ["INPUT_RESOURCE_NAME"]` and `function_name = resource_name`.
   - Update the ResourceNotFoundException error message (around line 253): Change `"Check ferry.yaml function_name or verify"` to `"Check ferry.yaml name field or verify"`.
   - All other uses of `function_name` as a local variable are fine -- it's the actual Lambda function name being passed to AWS APIs.

**action/src/ferry_action/deploy_stepfunctions.py:**

6. In `main()`:
   - Change `state_machine_name = os.environ["INPUT_STATE_MACHINE_NAME"]` to `state_machine_name = os.environ["INPUT_RESOURCE_NAME"]` (the resource name IS the state machine name).
   - Keep `resource_name = os.environ["INPUT_RESOURCE_NAME"]` as-is (already exists).
   - All other uses of `state_machine_name` as a local variable are fine -- it's used to construct the ARN and passed to AWS APIs.
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && .venv/bin/python -m pytest tests/test_action/test_parse_payload.py tests/test_action/test_deploy.py tests/test_action/test_deploy_stepfunctions.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
parse_payload matrix entries no longer include function_name or state_machine_name keys. deploy.py uses INPUT_RESOURCE_NAME as the function name. deploy_stepfunctions.py uses INPUT_RESOURCE_NAME as the state machine name. Tests will partially fail (expected -- updated in Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update composite actions, docs, and all action tests</name>
  <files>
    action/deploy/action.yml,
    action/deploy-stepfunctions/action.yml,
    docs/setup.md,
    tests/test_action/test_parse_payload.py,
    tests/test_action/test_deploy.py,
    tests/test_action/test_deploy_stepfunctions.py
  </files>
  <action>
**action/deploy/action.yml:**

1. Remove the `function-name` input entirely (lines 8-10).
2. Remove the `INPUT_FUNCTION_NAME: ${{ inputs.function-name }}` env var from the deploy step (line 70).
3. The `resource-name` input and `INPUT_RESOURCE_NAME` env var already exist and will serve as the function name.

**action/deploy-stepfunctions/action.yml:**

4. Remove the `state-machine-name` input entirely (lines 8-10).
5. Remove the `INPUT_STATE_MACHINE_NAME: ${{ inputs.state-machine-name }}` env var from the deploy step (line 69).
6. The `resource-name` input and `INPUT_RESOURCE_NAME` env var already exist and will serve as the state machine name.

**docs/setup.md:**

7. In the `ferry.yaml` example:
   - Lambda section: Remove the `function_name: order-processor` line and its comment. Update `name: order-processor` comment to say `# AWS Lambda function name (used for display and deploy)`. Keep all other fields.
   - Step Functions section: Remove the `state_machine_name: OrderWorkflow` line. Change `name: order-workflow` to `name: OrderWorkflow` (the name IS the AWS state machine name now). Update comment accordingly.
   - Add a note after the example: "`name` is used as the AWS resource name. For Lambdas, it is the Lambda function name. For Step Functions, it is the state machine name."

8. In the workflow template:
   - In the `deploy-lambda` job: Remove `function-name: ${{ matrix.function_name }}` from the "Deploy Lambda" step's `with:` block.
   - In the `deploy-step-function` job: Remove `state-machine-name: ${{ matrix.state_machine_name }}` from the "Deploy Step Functions" step's `with:` block.

9. In the "Migration from v1.x" section, add a bullet:
   - "**Schema simplification**: `function_name` (Lambdas) and `state_machine_name` (Step Functions) are removed. `name` is now the AWS resource name. Old ferry.yaml files with these fields still parse (backward compatible), but you should update them."

**tests/test_action/test_parse_payload.py** (19 function_name, 7 state_machine_name):

10. Remove `function_name=...` from ALL `LambdaResource()` constructors in test fixtures/helpers.
11. Remove `state_machine_name=...` from ALL `StepFunctionResource()` constructors.
12. Update ALL matrix entry assertions: where tests check `entry["function_name"]`, change to check `entry["name"]` (which should be the AWS function name). Same for `entry["state_machine_name"]` -> `entry["name"]`.
13. Remove any assertions that expect `"function_name"` or `"state_machine_name"` as keys in matrix dicts.

**tests/test_action/test_deploy.py** (4 function_name, 11 INPUT_FUNCTION_NAME):

14. Replace ALL `"INPUT_FUNCTION_NAME": "..."` entries in env var dicts/monkeypatches with just ensuring `"INPUT_RESOURCE_NAME": "..."` is set (it already is in most tests -- just remove INPUT_FUNCTION_NAME).
15. Remove any test that specifically validates INPUT_FUNCTION_NAME is required.
16. Update test assertions that reference `function_name` to use the resource name.

**tests/test_action/test_deploy_stepfunctions.py** (5 INPUT_STATE_MACHINE_NAME):

17. Replace ALL `"INPUT_STATE_MACHINE_NAME": "..."` entries in env var dicts with just ensuring `"INPUT_RESOURCE_NAME": "..."` is set. Remove INPUT_STATE_MACHINE_NAME entirely.
18. Update any test assertions that reference `state_machine_name` from env vars.

After all changes, run full test suite.
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && .venv/bin/python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
All 443+ tests pass. No test references function_name or state_machine_name as a dispatch model field or env var. Composite actions simplified. Docs updated with new workflow template and ferry.yaml examples.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Schema simplification across the entire ferry pipeline: ferry.yaml schema, dispatch models, action parsing, deploy scripts, composite actions, workflow template, and documentation. The `name` field is now the single source of truth for the AWS resource name (Lambda function name, State Machine name).
  </what-built>
  <how-to-verify>
1. **Verify tests pass**: `cd /Users/amit/Repos/github/ferry && .venv/bin/python -m pytest tests/ -q` -- all tests should pass
2. **Verify no stale references**: `grep -rn "function_name\|state_machine_name" backend/src/ utils/src/ action/src/ action/deploy/ action/deploy-stepfunctions/ docs/setup.md` -- should show zero hits (except possibly in backward-compat validator method names and local variable names in deploy scripts)
3. **Review ferry.yaml docs**: Open `docs/setup.md` and verify the example shows `name` as the AWS resource name without separate `function_name`/`state_machine_name` fields
4. **Review workflow template**: In `docs/setup.md`, verify deploy-lambda step no longer passes `function-name:` and deploy-step-function step no longer passes `state-machine-name:`
5. **Migrate ferry-test-app**: Push updated `ferry.yaml` and `.github/workflows/ferry.yml` to `AmitLaviDev/ferry-test-app` (see research doc for exact before/after). Then trigger a test to validate the full pipeline.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd /Users/amit/Repos/github/ferry && .venv/bin/python -m pytest tests/ -q` -- all tests pass
2. `grep -rn "\.function_name\b" backend/src/ utils/src/ action/src/` -- zero attribute access hits on models
3. `grep -rn "\.state_machine_name\b" backend/src/ utils/src/ action/src/` -- zero attribute access hits on models
4. `grep -rn "INPUT_FUNCTION_NAME\|INPUT_STATE_MACHINE_NAME" action/` -- zero hits
5. Docs and workflow template reference `matrix.name` not `matrix.function_name`/`matrix.state_machine_name`
</verification>

<success_criteria>
- parse_payload produces matrix entries with `name` as the AWS resource name (no function_name/state_machine_name keys)
- deploy.py reads INPUT_RESOURCE_NAME as the function name (INPUT_FUNCTION_NAME removed)
- deploy_stepfunctions.py reads INPUT_RESOURCE_NAME as the state machine name (INPUT_STATE_MACHINE_NAME removed)
- Composite action.yml files have no function-name or state-machine-name inputs
- Workflow template in docs/setup.md uses matrix.name only
- ferry.yaml documentation shows name as the AWS resource name
- All tests pass
- User has verified and migrated ferry-test-app
</success_criteria>

<output>
After completion, create `.planning/phases/37-schema-simplification/37-02-SUMMARY.md`
</output>
