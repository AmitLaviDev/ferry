---
phase: 10-docs-and-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/lambdas.md
  - docs/step-functions.md
  - docs/api-gateways.md
  - utils/src/ferry_utils/errors.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "All three workflow docs include checks:write in the permissions block"
    - "Lambda deploy step includes trigger-sha and github-token inputs"
    - "Step Functions and API Gateway deploy steps include github-token input"
    - "Permission lines use inline comments instead of block comment above"
    - "BuildError and DeployError classes no longer exist in the codebase"
  artifacts:
    - path: "docs/lambdas.md"
      provides: "Lambda workflow docs with complete permissions and deploy inputs"
      contains: "checks: write"
    - path: "docs/step-functions.md"
      provides: "Step Functions workflow docs with complete permissions and deploy inputs"
      contains: "checks: write"
    - path: "docs/api-gateways.md"
      provides: "API Gateway workflow docs with complete permissions and deploy inputs"
      contains: "checks: write"
    - path: "utils/src/ferry_utils/errors.py"
      provides: "Clean error hierarchy without unused classes"
      min_lines: 20
  key_links:
    - from: "docs/lambdas.md"
      to: "action/deploy/action.yml"
      via: "deploy step inputs match action inputs"
      pattern: "trigger-sha.*github-token"
    - from: "docs/step-functions.md"
      to: "action/deploy-stepfunctions/action.yml"
      via: "deploy step inputs match action inputs"
      pattern: "github-token"
    - from: "docs/api-gateways.md"
      to: "action/deploy-apigw/action.yml"
      via: "deploy step inputs match action inputs"
      pattern: "github-token"
---

<objective>
Fix workflow documentation gaps from Phase 8 and remove unused error classes.

Purpose: Phase 8 added Check Run reporting (report.py) which requires checks:write permission and github-token/trigger-sha inputs on deploy steps, but the workflow docs written in Phase 7 predated Phase 8 and were never backfilled. BuildError and DeployError were defined but never used -- action scripts communicate errors via exit codes and GHA annotations, not exception types.

Output: Updated docs/lambdas.md, docs/step-functions.md, docs/api-gateways.md with correct permissions and inputs; cleaned utils/src/ferry_utils/errors.py with dead code removed.
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-docs-and-dead-code-cleanup/10-CONTEXT.md
@.planning/phases/10-docs-and-dead-code-cleanup/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update workflow doc permissions and deploy inputs</name>
  <files>docs/lambdas.md, docs/step-functions.md, docs/api-gateways.md</files>
  <action>
In all three workflow docs, replace the existing permissions block:

```yaml
# OIDC requires id-token:write to request the JWT
# contents:read is needed to check out the repository
permissions:
  id-token: write
  contents: read
```

With per-line inline comments and the new checks:write permission:

```yaml
permissions:
  id-token: write    # OIDC JWT for AWS authentication
  contents: read     # Repository checkout
  checks: write      # Check Run status reporting
```

This removes the block comment above and converts to self-documenting inline comments per user decision.

Then update deploy step inputs:

**docs/lambdas.md** -- In the "Deploy Lambda" step, add two inputs:
- `trigger-sha: ${{ matrix.trigger_sha }}` with comment `# Git SHA for Check Run attachment` -- add after `deployment-tag` line
- `github-token: ${{ github.token }}` with comment `# Check Run reporting (auto-granted, not a PAT)` -- add after `aws-region` line

**docs/step-functions.md** -- In the "Deploy Step Functions" step, add one input:
- `github-token: ${{ github.token }}` with comment `# Check Run reporting (auto-granted, not a PAT)` -- add after `aws-region` line

**docs/api-gateways.md** -- In the "Deploy API Gateway" step, add one input:
- `github-token: ${{ github.token }}` with comment `# Check Run reporting (auto-granted, not a PAT)` -- add after `aws-region` line

IMPORTANT: The deploy github-token uses `${{ github.token }}` (auto-granted), NOT `${{ secrets.GH_PAT }}`. This is different from the build step's github-token which is for private repo deps.
  </action>
  <verify>
    <automated>grep -c "checks: write" docs/lambdas.md docs/step-functions.md docs/api-gateways.md && grep -c "github-token" docs/lambdas.md docs/step-functions.md docs/api-gateways.md && grep -c "trigger-sha" docs/lambdas.md && ! grep -q "# OIDC requires id-token:write" docs/lambdas.md docs/step-functions.md docs/api-gateways.md</automated>
    <manual>Verify each doc has 3-line permissions block with inline comments, and deploy steps include github-token (all 3) and trigger-sha (lambdas only)</manual>
  </verify>
  <done>All three workflow docs have checks:write permission with inline comments, Lambda deploy step has trigger-sha and github-token, SF and APIGW deploy steps have github-token, and the old block comment above permissions is removed</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused BuildError and DeployError classes</name>
  <files>utils/src/ferry_utils/errors.py</files>
  <action>
Remove the BuildError and DeployError class definitions from utils/src/ferry_utils/errors.py (lines 24-29). These classes are never imported or referenced from any other file in the codebase. The file should end after the ConfigError class definition (line 21), with a trailing newline.

The remaining error classes to keep are: FerryError, WebhookValidationError, DuplicateDeliveryError, GitHubAuthError, ConfigError.

No changes needed in utils/src/ferry_utils/__init__.py -- BuildError and DeployError are not exported there.
  </action>
  <verify>
    <automated>python -c "from ferry_utils.errors import FerryError, ConfigError; print('OK')" && ! python -c "from ferry_utils.errors import BuildError" 2>/dev/null && ! python -c "from ferry_utils.errors import DeployError" 2>/dev/null && echo "Dead code removed"</automated>
    <manual>Verify errors.py contains only FerryError, WebhookValidationError, DuplicateDeliveryError, GitHubAuthError, ConfigError</manual>
  </verify>
  <done>BuildError and DeployError class definitions are removed from errors.py; existing error classes remain intact and importable</done>
</task>

</tasks>

<verification>
1. `grep "checks: write" docs/lambdas.md docs/step-functions.md docs/api-gateways.md` -- all three files match
2. `grep "github-token.*github.token" docs/lambdas.md docs/step-functions.md docs/api-gateways.md` -- all three deploy steps include it
3. `grep "trigger-sha.*trigger_sha" docs/lambdas.md` -- Lambda deploy step includes it
4. `grep -r "BuildError\|DeployError" utils/ action/` -- zero matches (dead code fully removed)
5. `cd /Users/amit/Repos/github/ferry && uv run pytest tests/ -x -q` -- all existing tests pass
</verification>

<success_criteria>
- All three workflow docs include `checks: write` permission with inline comment
- docs/lambdas.md deploy step includes `trigger-sha` and `github-token` inputs
- docs/step-functions.md deploy step includes `github-token` input
- docs/api-gateways.md deploy step includes `github-token` input
- BuildError and DeployError are removed from ferry_utils/errors.py
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-and-dead-code-cleanup/10-01-SUMMARY.md`
</output>
