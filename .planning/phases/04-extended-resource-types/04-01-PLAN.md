---
phase: 04-extended-resource-types
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - action/src/ferry_action/envsubst.py
  - tests/test_action/test_envsubst.py
  - backend/src/ferry_backend/config/schema.py
  - tests/test_backend/test_config_schema.py
  - utils/src/ferry_utils/models/dispatch.py
  - backend/src/ferry_backend/dispatch/trigger.py
  - tests/test_backend/test_trigger.py
  - action/src/ferry_action/parse_payload.py
  - tests/test_action/test_parse_payload.py
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "envsubst replaces ${ACCOUNT_ID} and ${AWS_REGION} without corrupting JSONPath expressions or other dollar-prefixed content"
    - "StepFunctionConfig requires state_machine_name and definition_file fields"
    - "ApiGatewayConfig requires rest_api_id, stage_name, and spec_file fields"
    - "StepFunctionResource and ApiGatewayResource dispatch models carry type-specific deploy fields from config"
    - "parse_payload builds correct matrix entries for step_function and api_gateway resource types"
  artifacts:
    - path: "action/src/ferry_action/envsubst.py"
      provides: "Shared envsubst variable substitution + content hash helpers"
      exports: ["envsubst", "compute_content_hash", "get_content_hash_tag"]
    - path: "backend/src/ferry_backend/config/schema.py"
      provides: "Updated StepFunctionConfig and ApiGatewayConfig with deploy-specific fields"
      contains: "state_machine_name"
    - path: "utils/src/ferry_utils/models/dispatch.py"
      provides: "Updated StepFunctionResource and ApiGatewayResource with deploy-specific fields"
      contains: "state_machine_name"
    - path: "action/src/ferry_action/parse_payload.py"
      provides: "Extended matrix builder for all resource types"
      contains: "step_function"
    - path: "tests/test_action/test_envsubst.py"
      provides: "envsubst unit tests"
      min_lines: 40
  key_links:
    - from: "backend/src/ferry_backend/config/schema.py"
      to: "backend/src/ferry_backend/dispatch/trigger.py"
      via: "_build_resource maps config fields to dispatch resource fields"
      pattern: "sf\\.state_machine_name|sf\\.definition_file"
    - from: "utils/src/ferry_utils/models/dispatch.py"
      to: "action/src/ferry_action/parse_payload.py"
      via: "parse_payload reads dispatch resource fields into matrix"
      pattern: "StepFunctionResource|ApiGatewayResource"
---

<objective>
Create shared utilities (envsubst variable substitution, content hash helpers) and update config/dispatch/trigger/parse_payload models to carry Step Functions and API Gateway deploy-specific fields end-to-end from ferry.yaml to GHA matrix.

Purpose: Both deploy modules (Plans 02 and 03) need envsubst and content hashing. The dispatch pipeline (config -> trigger -> dispatch -> parse_payload) must carry type-specific fields so deploy actions receive the information they need.

Output: envsubst.py module with tests, updated config schema models, updated dispatch resource models, updated trigger._build_resource, extended parse_payload for all resource types.
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-extended-resource-types/04-CONTEXT.md
@.planning/phases/04-extended-resource-types/04-RESEARCH.md
@action/src/ferry_action/deploy.py
@action/src/ferry_action/parse_payload.py
@backend/src/ferry_backend/config/schema.py
@utils/src/ferry_utils/models/dispatch.py
@backend/src/ferry_backend/dispatch/trigger.py
@tests/test_action/test_parse_payload.py
@tests/test_backend/test_trigger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create envsubst module with content hash helpers (TDD)</name>
  <files>
    action/src/ferry_action/envsubst.py
    tests/test_action/test_envsubst.py
  </files>
  <action>
RED: Create `tests/test_action/test_envsubst.py` with failing tests for:
- `envsubst(content, account_id, region)` replaces `${ACCOUNT_ID}` and `${AWS_REGION}` in a string
- envsubst leaves JSONPath expressions like `$.input.path` untouched
- envsubst leaves `$PLAIN` and `${UNKNOWN_VAR}` untouched (only the two known variables)
- envsubst handles content with no variables (passthrough)
- envsubst handles content with multiple occurrences of the same variable
- `compute_content_hash(content)` returns a SHA-256 hex digest string
- Same content produces same hash (deterministic)
- Different content produces different hash
- `get_content_hash_tag(tags)` extracts `ferry:content-hash` value from Step Functions tag format `[{"key": "...", "value": "..."}]`
- `get_content_hash_tag(tags)` extracts from API Gateway tag format `{"ferry:content-hash": "abc123"}` (dict format)
- `get_content_hash_tag(tags)` returns `None` when tag not present

Run tests -- they MUST fail (module doesn't exist yet).

GREEN: Create `action/src/ferry_action/envsubst.py` with:
- `_ENVSUBST_PATTERN = re.compile(r"\$\{(ACCOUNT_ID|AWS_REGION)\}")` -- strict regex matching only these two variables
- `envsubst(content: str, account_id: str, region: str) -> str` -- uses `_ENVSUBST_PATTERN.sub` with a lambda that looks up replacements from a dict
- `compute_content_hash(content: str) -> str` -- `hashlib.sha256(content.encode("utf-8")).hexdigest()`
- `get_content_hash_tag(tags: list[dict] | dict) -> str | None` -- handles both SF tag format (list of dicts with "key"/"value") and APIGW tag format (flat dict). For list format, iterate and check `tag.get("key") == "ferry:content-hash"`. For dict format, just `tags.get("ferry:content-hash")`.

Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && uv run pytest tests/test_action/test_envsubst.py -x -v</automated>
  </verify>
  <done>envsubst replaces only ${ACCOUNT_ID} and ${AWS_REGION}, leaves JSONPath and unknown variables untouched. Content hash is deterministic SHA-256. Tag extraction handles both SF list and APIGW dict formats.</done>
</task>

<task type="auto">
  <name>Task 2: Update config schema, dispatch models, trigger, and parse_payload for SF/APIGW fields</name>
  <files>
    backend/src/ferry_backend/config/schema.py
    utils/src/ferry_utils/models/dispatch.py
    backend/src/ferry_backend/dispatch/trigger.py
    action/src/ferry_action/parse_payload.py
    tests/test_backend/test_config_schema.py
    tests/test_backend/test_trigger.py
    tests/test_action/test_parse_payload.py
  </files>
  <action>
**Config schema updates** (`backend/src/ferry_backend/config/schema.py`):
- Add to `StepFunctionConfig`: `state_machine_name: str` (required), `definition_file: str` (required)
- Add to `ApiGatewayConfig`: `rest_api_id: str` (required), `stage_name: str` (required), `spec_file: str` (required)
- All fields required, no defaults -- explicit over magical per user decision

**Dispatch model updates** (`utils/src/ferry_utils/models/dispatch.py`):
- Add to `StepFunctionResource`: `state_machine_name: str`, `definition_file: str`
- Add to `ApiGatewayResource`: `rest_api_id: str`, `stage_name: str`, `spec_file: str`
- These fields carry the config values through the dispatch payload so the action has everything it needs

**Trigger updates** (`backend/src/ferry_backend/dispatch/trigger.py`):
- In `_build_resource()`, update the `step_function` branch to pass `state_machine_name=sf.state_machine_name, definition_file=sf.definition_file`
- Update the `api_gateway` branch to pass `rest_api_id=ag.rest_api_id, stage_name=ag.stage_name, spec_file=ag.spec_file`

**parse_payload updates** (`action/src/ferry_action/parse_payload.py`):
- Import `StepFunctionResource` and `ApiGatewayResource` from `ferry_utils.models.dispatch`
- In `build_matrix()`, detect `payload.resource_type`:
  - `"lambda"`: existing logic (filter to LambdaResource, include ecr/runtime fields)
  - `"step_function"`: filter to StepFunctionResource, matrix entries include: name, source, state_machine_name, definition_file, trigger_sha, deployment_tag
  - `"api_gateway"`: filter to ApiGatewayResource, matrix entries include: name, source, rest_api_id, stage_name, spec_file, trigger_sha, deployment_tag
- No `ecr` or `runtime` fields for SF/APIGW (those are Lambda-specific)

**Test updates**:
- Update `tests/test_backend/test_config_schema.py`: add tests for new required fields on StepFunctionConfig and ApiGatewayConfig (validation passes with fields, fails without them). Update any existing tests that construct SF/APIGW configs without the new required fields -- add the required fields.
- Update `tests/test_backend/test_trigger.py`: update any existing SF/APIGW test fixtures to include new required config fields. Add test that `_build_resource` passes new fields through to dispatch models.
- Update `tests/test_action/test_parse_payload.py`: add tests for SF and APIGW matrix building. Verify matrix entries contain type-specific fields and do NOT contain Lambda-specific fields (ecr, runtime).
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && uv run pytest tests/test_backend/test_config_schema.py tests/test_backend/test_trigger.py tests/test_action/test_parse_payload.py -x -v</automated>
  </verify>
  <done>StepFunctionConfig requires state_machine_name + definition_file. ApiGatewayConfig requires rest_api_id + stage_name + spec_file. Dispatch models carry these fields. trigger._build_resource maps config fields to dispatch. parse_payload builds correct type-specific matrix entries for all three resource types. All existing tests still pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/amit/Repos/github/ferry && uv run pytest tests/test_action/test_envsubst.py tests/test_backend/test_config_schema.py tests/test_backend/test_trigger.py tests/test_action/test_parse_payload.py -x -v
```
```bash
cd /Users/amit/Repos/github/ferry && uv run ruff check action/src/ferry_action/envsubst.py backend/src/ferry_backend/config/schema.py utils/src/ferry_utils/models/dispatch.py backend/src/ferry_backend/dispatch/trigger.py action/src/ferry_action/parse_payload.py
```
</verification>

<success_criteria>
- envsubst.py module exists with envsubst(), compute_content_hash(), get_content_hash_tag() functions
- envsubst handles ${ACCOUNT_ID}, ${AWS_REGION} substitution without corrupting JSONPath
- StepFunctionConfig has state_machine_name and definition_file as required fields
- ApiGatewayConfig has rest_api_id, stage_name, and spec_file as required fields
- StepFunctionResource and ApiGatewayResource dispatch models carry type-specific fields
- _build_resource maps all new config fields to dispatch resource models
- parse_payload.build_matrix handles lambda, step_function, and api_gateway resource types
- All tests pass, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-extended-resource-types/04-01-SUMMARY.md`
</output>
