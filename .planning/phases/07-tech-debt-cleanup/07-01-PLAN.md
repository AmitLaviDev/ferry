---
phase: 07-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/ferry_backend/config/schema.py
  - utils/src/ferry_utils/models/dispatch.py
  - backend/src/ferry_backend/dispatch/trigger.py
  - action/src/ferry_action/parse_payload.py
  - action/build/action.yml
  - action/Dockerfile
  - tests/test_backend/test_config_schema.py
  - tests/test_utils/test_dispatch_models.py
  - tests/test_backend/test_dispatch_trigger.py
  - tests/test_action/test_parse_payload.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "LambdaConfig.runtime defaults to python3.14 (single source of truth)"
    - "runtime flows from ferry.yaml through dispatch pipeline to GHA matrix without any hardcoded fallback in parse_payload.py"
    - "Dockerfile ARG PYTHON_VERSION and action.yml runtime input default both say 3.14 / python3.14"
    - "All tests pass with updated runtime values"
  artifacts:
    - path: "utils/src/ferry_utils/models/dispatch.py"
      provides: "LambdaResource with runtime: str field"
      contains: "runtime: str"
    - path: "backend/src/ferry_backend/config/schema.py"
      provides: "LambdaConfig with python3.14 default"
      contains: "python3.14"
    - path: "action/src/ferry_action/parse_payload.py"
      provides: "Lambda matrix reads runtime from dispatch model"
      contains: "r.runtime"
    - path: "action/Dockerfile"
      provides: "Dockerfile with 3.14 default"
      contains: "PYTHON_VERSION=3.14"
    - path: "action/build/action.yml"
      provides: "Composite action with python3.14 default"
      contains: "python3.14"
  key_links:
    - from: "backend/src/ferry_backend/config/schema.py"
      to: "utils/src/ferry_utils/models/dispatch.py"
      via: "_build_resource passes lam.runtime to LambdaResource"
      pattern: "runtime=lam\\.runtime"
    - from: "utils/src/ferry_utils/models/dispatch.py"
      to: "action/src/ferry_action/parse_payload.py"
      via: "matrix builder reads r.runtime from dispatch model"
      pattern: "r\\.runtime"
---

<objective>
Wire `runtime` end-to-end through the Lambda dispatch pipeline and unify all defaults to `python3.14`.

Purpose: Resolve the runtime default inconsistency (python3.10 in schema, python3.12 in parse_payload/action.yml/Dockerfile) by establishing `LambdaConfig.runtime` as the single source of truth and flowing the value through the dispatch pipeline -- the same pattern proven in Phase 6 for `function_name`.

Output: `runtime` field on `LambdaResource`, updated defaults everywhere, no hardcoded runtime in parse_payload.py, all tests green.
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-cleanup/07-RESEARCH.md
@.planning/phases/06-fix-lambda-function-name-pipeline/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire runtime through source files and update defaults to python3.14</name>
  <files>
    backend/src/ferry_backend/config/schema.py
    utils/src/ferry_utils/models/dispatch.py
    backend/src/ferry_backend/dispatch/trigger.py
    action/src/ferry_action/parse_payload.py
    action/build/action.yml
    action/Dockerfile
  </files>
  <action>
    Follow the exact Phase 6 `function_name` wiring pattern for `runtime`:

    1. **schema.py line 23**: Change `runtime: str = "python3.10"` to `runtime: str = "python3.14"` -- this is the single source of truth for the default.

    2. **dispatch.py LambdaResource**: Add `runtime: str` as a required field (after `function_name`). No default value -- the backend resolves the default before constructing the dispatch model (same rationale as function_name in Phase 6).

    3. **trigger.py _build_resource lambda branch**: Add `runtime=lam.runtime` to the `LambdaResource(...)` constructor call (after `function_name=lam.function_name`).

    4. **parse_payload.py _build_lambda_matrix**: Replace `"runtime": "python3.12"` (the hardcoded tech debt) with `"runtime": r.runtime` to read from the dispatch model. Also update the docstring on `_build_lambda_matrix` (lines 24-31) -- remove the stale note about "dispatch payload intentionally does not include runtime" and replace with a note that runtime flows from the dispatch payload (source of truth: LambdaConfig). Also update the `build_matrix` docstring (line 101) to add `function_name` to the lambda field list (already flagged by Phase 6 verification).

    5. **action/build/action.yml line 30**: Change `default: "python3.12"` to `default: "python3.14"` for the runtime input. This is a fallback for direct usage outside Ferry; it must match the canonical default.

    6. **action/Dockerfile line 2**: Change `ARG PYTHON_VERSION=3.12` to `ARG PYTHON_VERSION=3.14` so manual builds match the canonical default.
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && grep -n "python3.14" backend/src/ferry_backend/config/schema.py && grep -n "runtime: str" utils/src/ferry_utils/models/dispatch.py && grep -n "r.runtime" action/src/ferry_action/parse_payload.py && grep -n "python3.14" action/build/action.yml && grep -n "3.14" action/Dockerfile</automated>
    <manual>Verify no occurrences of "python3.10" or "python3.12" remain as defaults in source files (only in tests or explicit overrides)</manual>
  </verify>
  <done>LambdaConfig defaults to python3.14, LambdaResource has runtime field, trigger passes runtime, parse_payload reads from model (no hardcoded value), action.yml and Dockerfile defaults are 3.14</done>
</task>

<task type="auto">
  <name>Task 2: Update all tests for runtime field addition and new default</name>
  <files>
    tests/test_backend/test_config_schema.py
    tests/test_utils/test_dispatch_models.py
    tests/test_backend/test_dispatch_trigger.py
    tests/test_action/test_parse_payload.py
  </files>
  <action>
    Systematically update every test that is affected by the runtime changes. Search for ALL occurrences of `python3.10` and `python3.12` in test files to find every assertion or construction site.

    1. **test_config_schema.py**: Lines 29, 32 assert `python3.10` as the default runtime. Change to `python3.14`. Leave tests in test_changes.py alone -- they use explicit runtime values for diffing, not the default.

    2. **test_dispatch_models.py**: Add `runtime="python3.14"` (or any valid string) to ALL `LambdaResource(...)` constructions. Follow the same pattern as Phase 6 did for `function_name`: every existing construction needs the new required field. Add a dedicated test that `LambdaResource` requires `runtime` (missing field raises ValidationError).

    3. **test_dispatch_trigger.py**: Assert that `_build_resource` for lambda includes `runtime` in the returned model. The constructed LambdaResource should have `runtime` matching the config's runtime value. Add a test for runtime override (explicit runtime in config flows through).

    4. **test_parse_payload.py**: Line 66 asserts `"runtime": "python3.12"` in the matrix output. Update to assert `"runtime": "python3.14"` (the value from the dispatch model, which now carries the config default). Also update all LambdaResource constructions in test payload builders to include `runtime=`.

    **Important**: Do NOT change tests in test_build.py (lines 177, 246 use `INPUT_RUNTIME` env var with explicit values -- they test the build module's runtime parsing, not the pipeline default).
    Do NOT change tests in test_changes.py (lines 297, 307, 364, 382 use explicit runtime values for change detection diffing -- they don't rely on the default).
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. No test asserts python3.10 or python3.12 as a default value. LambdaResource constructions in tests include runtime field. Runtime pipeline tests verify end-to-end flow.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/ -x -q` -- all tests pass
2. `grep -rn "python3.10" backend/ utils/ action/src/` returns no hits in source files
3. `grep -rn '"python3.12"' action/src/ferry_action/parse_payload.py` returns no hits (hardcoded value removed)
4. `grep -rn "runtime" utils/src/ferry_utils/models/dispatch.py` shows `runtime: str` on LambdaResource
</verification>

<success_criteria>
- LambdaConfig.runtime defaults to python3.14
- LambdaResource carries runtime as required str field
- _build_resource passes runtime from config to dispatch model
- parse_payload reads runtime from dispatch model (no hardcoded fallback)
- action.yml and Dockerfile defaults are python3.14 / 3.14
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md`
</output>
