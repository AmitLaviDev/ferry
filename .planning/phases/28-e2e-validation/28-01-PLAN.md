---
phase: 28-e2e-validation
plan: 01
type: execute
wave: 1
depends_on: [26-01, 27-01]
files_modified: []
autonomous: false
requirements: [VAL-01, VAL-02]

must_haves:
  truths:
    - "A push touching all 3 resource types produces exactly 1 workflow run with 3 active deploy jobs and 0 skipped"
    - "A push touching only 1 resource type produces exactly 1 workflow run with 1 active deploy job and 2 skipped"
    - "Backend sends batched v2 payload (single workflow_dispatch per push)"
  artifacts: []
  key_links:
    - from: "test repo push (all 3 types)"
      to: "single ferry.yml dispatch"
      via: "Ferry webhook → batched dispatch → workflow_dispatch"
      pattern: "1 run, 3 active jobs"
    - from: "test repo push (1 type)"
      to: "single ferry.yml dispatch"
      via: "Ferry webhook → batched dispatch → workflow_dispatch"
      pattern: "1 run, 1 active job, 2 skipped"
---

<objective>
Prove batched dispatch works end-to-end: one push → one workflow_dispatch → one workflow run with correct job routing.

Purpose: Validate that phases 25-27 (shared models, backend batched dispatch, action parsing + workflow template) work together in production.
Output: Two successful E2E test runs — single-type and multi-type — proving VAL-02 and VAL-01 respectively.
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-e2e-validation/28-CONTEXT.md
@.planning/phases/27-action-parsing-and-workflow-template/27-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Push ferry monorepo to main and wait for self-deploy</name>
  <action>
Push all phases 25-27 code to the ferry remote. The self-deploy workflow triggers on `backend/**` changes (from phase 26) and will redeploy the backend Lambda with the batched dispatch logic. Action code at `@main` ref picks up phase 27 changes automatically after push.

**Step A: Verify local is ahead of remote**
```bash
cd /Users/amit/Repos/github/ferry
git log origin/main..HEAD --oneline
```
Should show commits from phases 25-27.

**Step B: Push to main**
```bash
git push origin main
```

**Step C: Wait for self-deploy to trigger and complete** (up to 5 min)
```bash
# Check self-deploy workflow status
gh run list --repo AmitLaviDev/ferry --workflow self-deploy.yml --limit 3
# Once a run appears, watch it
gh run view <RUN_ID> --repo AmitLaviDev/ferry
```
All jobs (test + deploy) should complete successfully.

**Step D: Verify backend Lambda is updated**
```bash
eval $(aws configure export-credentials --profile amit-private --format env)
aws lambda get-function --function-name ferry-backend --query 'Configuration.LastModified'
```
LastModified should be very recent (within the last few minutes).

**If self-deploy fails:** Check `gh run view <RUN_ID> --repo AmitLaviDev/ferry --log-failed`, fix in ferry repo, push again.
  </action>
  <verify>Self-deploy completed successfully and backend Lambda is updated with batched dispatch code</verify>
  <done>Ferry monorepo pushed to main, self-deploy green, backend Lambda updated</done>
  <resume-signal>Report self-deploy status. "Self-deploy green" if all jobs passed, or describe failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Update test repo ferry.yml to v1.5 template</name>
  <action>
Replace the v1.4 workflow file with the v1.5 batched dispatch template. This is a standalone commit+push so any dispatch it triggers settles before we push validation changes.

**Step A: Replace ferry.yml with v1.5 template**

Replace the entire contents of `/Users/amit/Repos/github/ferry-test-app/.github/workflows/ferry.yml` with:

```yaml
# .github/workflows/ferry.yml
name: Ferry Deploy

run-name: "Ferry Deploy: ${{ github.event.inputs.payload && (fromJson(github.event.inputs.payload).resource_types || fromJson(github.event.inputs.payload).resource_type || 'dispatched') || 'manual' }}"

on:
  workflow_dispatch:
    inputs:
      payload:
        description: "Ferry dispatch payload (JSON) -- sent by Ferry App, not for manual use"
        required: true

env:
  AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
  AWS_REGION: us-east-1

permissions:
  id-token: write
  contents: read
  checks: write

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      has_lambdas: ${{ steps.parse.outputs.has_lambdas }}
      has_step_functions: ${{ steps.parse.outputs.has_step_functions }}
      has_api_gateways: ${{ steps.parse.outputs.has_api_gateways }}
      lambda_matrix: ${{ steps.parse.outputs.lambda_matrix }}
      sf_matrix: ${{ steps.parse.outputs.sf_matrix }}
      ag_matrix: ${{ steps.parse.outputs.ag_matrix }}
      resource_types: ${{ steps.parse.outputs.resource_types }}
    steps:
      - uses: actions/checkout@v4
      - name: Parse Ferry payload
        id: parse
        uses: AmitLaviDev/ferry/action/setup@main
        with:
          payload: ${{ inputs.payload }}

  deploy-lambda:
    name: "Ferry: deploy ${{ matrix.name }}"
    needs: setup
    if: needs.setup.outputs.has_lambdas == 'true'
    runs-on: ubuntu-latest
    concurrency:
      group: ferry-deploy-lambda-${{ matrix.name }}
      cancel-in-progress: false
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.lambda_matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Build container
        id: build
        uses: AmitLaviDev/ferry/action/build@main
        with:
          resource-name: ${{ matrix.name }}
          source-dir: ${{ matrix.source }}
          ecr-repo: ${{ matrix.ecr }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          trigger-sha: ${{ matrix.trigger_sha }}
          deployment-tag: ${{ matrix.deployment_tag }}
          runtime: ${{ matrix.runtime }}
      - name: Deploy Lambda
        uses: AmitLaviDev/ferry/action/deploy@main
        with:
          resource-name: ${{ matrix.name }}
          function-name: ${{ matrix.function_name }}
          image-uri: ${{ steps.build.outputs.image-uri }}
          image-digest: ${{ steps.build.outputs.image-digest }}
          deployment-tag: ${{ matrix.deployment_tag }}
          trigger-sha: ${{ matrix.trigger_sha }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          github-token: ${{ github.token }}

  deploy-step-function:
    name: "Ferry: deploy ${{ matrix.name }}"
    needs: setup
    if: needs.setup.outputs.has_step_functions == 'true'
    runs-on: ubuntu-latest
    concurrency:
      group: ferry-deploy-step-function-${{ matrix.name }}
      cancel-in-progress: false
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.sf_matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Step Functions
        uses: AmitLaviDev/ferry/action/deploy-stepfunctions@main
        with:
          resource-name: ${{ matrix.name }}
          state-machine-name: ${{ matrix.state_machine_name }}
          definition-file: ${{ matrix.definition_file }}
          source-dir: ${{ matrix.source }}
          trigger-sha: ${{ matrix.trigger_sha }}
          deployment-tag: ${{ matrix.deployment_tag }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          github-token: ${{ github.token }}

  deploy-api-gateway:
    name: "Ferry: deploy ${{ matrix.name }}"
    needs: setup
    if: needs.setup.outputs.has_api_gateways == 'true'
    runs-on: ubuntu-latest
    concurrency:
      group: ferry-deploy-api-gateway-${{ matrix.name }}
      cancel-in-progress: false
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.ag_matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Deploy API Gateway
        uses: AmitLaviDev/ferry/action/deploy-apigw@main
        with:
          resource-name: ${{ matrix.name }}
          rest-api-id: ${{ matrix.rest_api_id }}
          stage-name: ${{ matrix.stage_name }}
          spec-file: ${{ matrix.spec_file }}
          source-dir: ${{ matrix.source }}
          trigger-sha: ${{ matrix.trigger_sha }}
          deployment-tag: ${{ matrix.deployment_tag }}
          aws-role-arn: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          github-token: ${{ github.token }}
```

Key differences from v1.4:
- `run-name` uses `resource_types` (v2) with `resource_type` (v1) fallback
- Setup outputs 7 fields (3 booleans + 3 matrices + resource_types) instead of 2 (matrix + resource_type)
- Deploy jobs gate on `has_lambdas == 'true'` instead of `resource_type == 'lambda'`
- Each deploy job uses its own per-type matrix (`lambda_matrix`, `sf_matrix`, `ag_matrix`)
- Concurrency groups include `${{ matrix.name }}` for per-resource granularity

**Step B: Commit and push**
```bash
cd /Users/amit/Repos/github/ferry-test-app
git add .github/workflows/ferry.yml
git commit -m "feat: update ferry.yml to v1.5 batched dispatch template"
git push origin main
```

**Step C: Wait for any dispatch to settle** (1-2 min)
The workflow file change itself will trigger a push webhook to Ferry. Ferry will see no resource directories changed and should NOT dispatch. Verify:
```bash
# Check no new workflow runs were triggered
gh run list --repo AmitLaviDev/ferry-test-app --limit 3
```
If a run was triggered, wait for it to complete before proceeding.

**If push triggers an unexpected dispatch:** This is fine — the workflow file change doesn't affect resource source directories, so Ferry should ignore it. If it doesn't, investigate backend logs.
  </action>
  <verify>Test repo ferry.yml updated to v1.5 template and no unexpected workflow runs triggered</verify>
  <done>Test repo ferry.yml updated to v1.5 batched dispatch template, no spurious dispatches</done>
  <resume-signal>Report push result. "Template updated, no dispatch" if clean, or describe any unexpected runs.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Single-type validation (VAL-02)</name>
  <action>
Push a dummy change to only ONE resource source directory. Verify Ferry sends a batched v2 payload but with only 1 type, resulting in 1 active deploy job and 2 skipped.

**Step A: Make a trivial change to the Lambda source only**
```bash
cd /Users/amit/Repos/github/ferry-test-app
```
Edit `lambdas/hello-world/main.py` — add or update a comment to ensure a fresh change:
```python
# Ferry v1.5 single-type E2E validation
```
This changes only the `lambdas/hello-world/` directory. SF and APGW directories are untouched.

**Step B: Commit and push**
```bash
git add lambdas/hello-world/main.py
git commit -m "test: single-type E2E validation for Ferry v1.5 batched dispatch"
git push origin main
```

**Step C: Verify exactly 1 workflow run was triggered** (wait 30s for dispatch)
```bash
gh run list --repo AmitLaviDev/ferry-test-app --branch main --limit 5
```
There should be exactly 1 new run. The run-name should show `Ferry Deploy: lambda` (resource_types for a single-type batch).

**Step D: Verify job routing — 1 active, 2 skipped**
```bash
gh run view <RUN_ID> --repo AmitLaviDev/ferry-test-app --json jobs --jq '.jobs[] | {name: .name, conclusion: .conclusion, status: .status}'
```
Expected:
- `setup` — completed/success
- `Ferry: deploy hello-world` (Lambda) — completed/success
- `Ferry: deploy hello-chain` (SF) — skipped
- `Ferry: deploy hello-chain` (APGW) — skipped

**Step E: Verify backend logs show batched dispatch**
```bash
eval $(aws configure export-credentials --profile amit-private --format env)
aws logs tail /aws/lambda/ferry-backend --since 10m --format short | grep -E 'dispatch|batched|payload'
```
Look for: `mode=batched` in dispatch log events, single dispatch call (not per-type).

**If validation fails:**
1. If 0 runs: check backend logs for webhook receipt and dispatch errors
2. If >1 runs: backend is still dispatching per-type — check `trigger.py` deployment
3. If wrong jobs active/skipped: check action parse_payload output in setup job logs
  </action>
  <verify>Exactly 1 workflow run triggered, Lambda deploy job succeeded, SF and APGW jobs skipped</verify>
  <done>VAL-02 passed: single-type push → 1 run, 1 active Lambda job, 2 skipped (SF + APGW)</done>
  <resume-signal>Report run results. "VAL-02 passed" with run ID if 1 run + correct job routing, or describe failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Multi-type validation (VAL-01)</name>
  <action>
Push a single commit touching all 3 resource source directories. Verify Ferry sends a batched v2 payload with all 3 types, resulting in all 3 deploy jobs active.

**Step A: Make trivial changes to ALL 3 resource directories**
```bash
cd /Users/amit/Repos/github/ferry-test-app
```

Edit `lambdas/hello-world/main.py` — add or update a comment:
```python
# Ferry v1.5 multi-type E2E validation
```

Edit `workflows/hello-chain/definition.json` — update the Comment field:
```json
"Comment": "Ferry v1.5 multi-type E2E validation"
```

Edit `api/hello-chain/openapi.yaml` — update the description:
```yaml
description: "Ferry v1.5 multi-type E2E validation"
```

**Step B: Stage all 3 changes and push as a single commit**
```bash
git add lambdas/hello-world/main.py workflows/hello-chain/definition.json api/hello-chain/openapi.yaml
git commit -m "test: multi-type E2E validation for Ferry v1.5 batched dispatch"
git push origin main
```

**Step C: Verify exactly 1 workflow run was triggered** (wait 30s for dispatch)
```bash
gh run list --repo AmitLaviDev/ferry-test-app --branch main --limit 5
```
There should be exactly 1 new run. The run-name should show `Ferry Deploy: lambda,step_function,api_gateway` (all 3 types in resource_types).

**Step D: Verify job routing — all 3 active, 0 skipped**
```bash
gh run view <RUN_ID> --repo AmitLaviDev/ferry-test-app --json jobs --jq '.jobs[] | {name: .name, conclusion: .conclusion, status: .status}'
```
Expected:
- `setup` — completed/success
- `Ferry: deploy hello-world` (Lambda) — completed/success
- `Ferry: deploy hello-chain` (SF) — completed/success
- `Ferry: deploy hello-chain` (APGW) — completed/success

**Step E: Verify backend logs show batched dispatch with all types**
```bash
eval $(aws configure export-credentials --profile amit-private --format env)
aws logs tail /aws/lambda/ferry-backend --since 10m --format short | grep -E 'dispatch|batched|payload'
```
Look for: `mode=batched` and all 3 types in the dispatch log events.

**If validation fails:**
1. If 0 runs: check backend logs
2. If >1 runs: backend is falling back to per-type — check payload size or dispatch logic
3. If some jobs failed: check individual job logs via `gh run view <RUN_ID> --repo AmitLaviDev/ferry-test-app --log-failed`
4. If some jobs skipped when they should be active: check setup job output for boolean flags
  </action>
  <verify>Exactly 1 workflow run triggered, all 3 deploy jobs completed successfully, 0 skipped</verify>
  <done>VAL-01 passed: multi-type push → 1 run, 3 active deploy jobs (Lambda + SF + APGW), 0 skipped</done>
  <resume-signal>Report run results. "VAL-01 passed" with run ID if 1 run + all 3 jobs active, or describe failure.</resume-signal>
</task>

</tasks>

<verification>
- Ferry monorepo pushed to main and self-deploy completed
- Test repo ferry.yml updated to v1.5 batched dispatch template
- Single-type push produces 1 run with 1 active + 2 skipped (VAL-02)
- Multi-type push produces 1 run with 3 active + 0 skipped (VAL-01)
- Backend logs confirm batched v2 dispatch (mode=batched) for both pushes
</verification>

<success_criteria>
- VAL-02: Single-type push → exactly 1 workflow run, 1 active deploy job, 2 skipped deploy jobs
- VAL-01: Multi-type push → exactly 1 workflow run, 3 active deploy jobs, 0 skipped deploy jobs
- No per-type dispatch fallback (backend uses batched path for both pushes)
- All active deploy jobs complete successfully
</success_criteria>

<output>
After completion, create `.planning/phases/28-e2e-validation/28-01-SUMMARY.md`
</output>
