# v1.3 Full-Chain E2E — Archived Roadmap

**Shipped:** 2026-03-10
**Timeline:** 2026-03-08 → 2026-03-10 (3 days)
**Git range:** v1.2..v1.3 (20 commits)
**Code changes:** 32 files, +705/-2755 lines

## Milestone Goal

Prove Step Functions and API Gateway deploy paths end-to-end with an integrated chain (API Gateway → Step Function → Lambda), and clean up v1.2 tech debt.

## Key Accomplishments

1. **Cleaned 5 tech debt items from v1.2** — error handling, IAM permissions, Docker warnings, workflow docs
2. **Built SF + APGW test infrastructure** — state machine, REST API, execution roles, deploy permissions (Terraform)
3. **Proved all 3 deploy paths** — Lambda, Step Functions, and API Gateway each independently deploy via Ferry dispatch
4. **Validated full chain: APGW → SF → Lambda** — POST /execute triggers SF which invokes Lambda, data traced through all layers
5. **Two-level skip detection works** — dispatch-level (no affected resources → no dispatch) and deploy-level (content-hash unchanged → skip deploy)
6. **Multi-type dispatch proven** — single push changing all 3 types triggers 3 parallel dispatches, all succeed

## Phases

- [x] **Phase 18: Tech Debt Cleanup** (2/2 plans) -- completed 2026-03-08
  - 18-01: Fix deploy.py error handling + build.py Docker warning (TD-01, TD-04, TD-05)
  - 18-02: Add IAM permission + fix workflow docs (TD-02, TD-03)
- [x] **Phase 19: Test Infrastructure for SF + APGW** (1/1 plan) -- completed 2026-03-08
  - 19-01: SF state machine + REST API + execution roles + deploy permissions
- [x] **Phase 20: Test Repo Updates** (1/1 plan) -- completed 2026-03-09
  - 20-01: ASL definition, OpenAPI spec, ferry.yaml, SF + APGW workflow files
- [x] **Phase 21: Full-Chain E2E Validation** (3/3 plans) -- completed 2026-03-10
  - 21-01: Pre-push Lambda echo + individual SF/APGW deploy verification
  - 21-02: Full chain invocation + no-op skip + multi-type dispatch
  - 21-03: Validation report

## Phase Details

### Phase 18: Tech Debt Cleanup
**Goal**: Clean up all pending tech debt items carried forward from v1.2
**Requirements**: TD-01, TD-02, TD-03, TD-04, TD-05
**Success Criteria**:
  1. No debug logging statements remain in deploy.py
  2. Self-deploy IAM policy includes `lambda:GetFunctionConfiguration`
  3. Workflow template docs show `name:` field on deploy jobs
  4. Docker credential warning is suppressed in build.py
  5. deploy.py error mapping distinguishes caller vs target role permission failures
  6. All tests pass, no lint errors

### Phase 19: Test Infrastructure for SF + APGW
**Goal**: AWS resources exist for Step Functions and API Gateway testing
**Requirements**: INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06
**Success Criteria**:
  1. Standard Step Functions state machine exists with a placeholder definition
  2. REST API exists with a `test` stage
  3. SF execution role has `lambda:InvokeFunction` permission on the test Lambda
  4. API Gateway has IAM integration permission to call `states:StartExecution`
  5. `ferry-test-deploy` role has Step Functions deploy permissions
  6. `ferry-test-deploy` role has API Gateway deploy permissions
  7. `terraform plan` shows no pending changes after apply

### Phase 20: Test Repo Updates
**Goal**: Test repo contains all files needed to exercise SF and APGW deploy paths
**Requirements**: REPO-01, REPO-02, REPO-03, REPO-04, REPO-05
**Success Criteria**:
  1. ASL definition with Task state invoking the test Lambda
  2. OpenAPI spec with POST endpoint triggering SF
  3. ferry.yaml has `step_functions` and `api_gateways` sections
  4. `.github/workflows/ferry-step_functions.yml` exists
  5. `.github/workflows/ferry-api_gateways.yml` exists

### Phase 21: Full-Chain E2E Validation
**Goal**: The full integrated chain works — APGW → SF → Lambda — deployed via Ferry
**Requirements**: E2E-01, E2E-02, E2E-03, E2E-04, E2E-05, E2E-06, E2E-07
**Success Criteria**:
  1. SF definition change triggers Ferry → dispatch → deploy
  2. APGW spec change triggers Ferry → dispatch → deploy
  3. APGW POST endpoint → SF → Lambda chain completes successfully
  4. No-op push correctly skips deploys via content-hash
  5. Multi-type push triggers 3 separate dispatches, all succeed

## Bugs Found and Fixed

### Phase 20 debugging session (5 bugs)
1. SF deploy: missing `states:PublishStateMachineVersion` — added to test_sf_deploy policy
2. APGW deploy: wrong IAM action names — API Gateway uses HTTP-verb actions, not SDK method names
3. APGW deploy: missing `iam:PassRole` — OpenAPI spec with `credentials:` needs PassRole
4. APGW deploy: tags use different resource ARN — `/tags/{url-encoded-resource-arn}`
5. Terraform fighting Ferry deploys — added `lifecycle { ignore_changes }` for definition/body and content-hash tags

### Phase 21 (0 bugs)
All deploy paths and chain invocation worked on first attempt.

## Performance

| Phase | Plan | Duration | Tasks | Files |
|-------|------|----------|-------|-------|
| 18-01 | Action Code Tech Debt | 219s | 2 | 4 |
| 18-02 | IAM + Workflow Docs | ~60s | 2 | 2 |
| 19-01 | SF + APGW Test Infra TF | 195s | 4 | 7 |
| 20-01 | Test Repo SF + APGW Files | ~45s | 2 | 5 |
| 21-01 | Deploy Paths Verification | manual | 3 | 0 |
| 21-02 | Chain + Skip + Multi-Type | manual | 3 | 0 |
| 21-03 | Validation Report | manual | 1 | 1 |
