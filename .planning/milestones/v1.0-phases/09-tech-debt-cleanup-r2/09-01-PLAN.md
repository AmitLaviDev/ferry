---
phase: 09-tech-debt-cleanup-r2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - utils/pyproject.toml
  - pyproject.toml
  - utils/src/ferry_utils/__init__.py
  - utils/src/ferry_utils/models/__init__.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "tenacity is not declared in any pyproject.toml"
    - "PyYAML is declared in backend/pyproject.toml and NOT in utils/pyproject.toml"
    - "moto extras include stepfunctions in root pyproject.toml"
    - "PushEvent and WebhookHeaders are not in __all__ of ferry_utils or ferry_utils.models"
    - "All existing tests pass after dependency and export changes"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Correct backend dependencies (PyYAML added, tenacity removed)"
      contains: "PyYAML>=6.0.1"
    - path: "utils/pyproject.toml"
      provides: "Correct utils dependencies (PyYAML removed)"
    - path: "pyproject.toml"
      provides: "Correct moto extras (stepfunctions added)"
      contains: "moto[dynamodb,apigateway,stepfunctions]"
    - path: "utils/src/ferry_utils/__init__.py"
      provides: "Clean exports without PushEvent/WebhookHeaders"
    - path: "utils/src/ferry_utils/models/__init__.py"
      provides: "Clean model exports without PushEvent/WebhookHeaders"
  key_links:
    - from: "backend/src/ferry_backend/config/loader.py"
      to: "backend/pyproject.toml"
      via: "PyYAML dependency declaration"
      pattern: "PyYAML"
    - from: "tests/test_deploy_stepfunctions.py"
      to: "pyproject.toml"
      via: "moto stepfunctions extra"
      pattern: "stepfunctions"
---

<objective>
Resolve four remaining tech debt items from the second milestone audit: remove phantom tenacity dependency, move PyYAML to correct package, add moto stepfunctions extra, and clean unused PushEvent/WebhookHeaders from public exports.

Note: The fifth audit item (build_matrix docstring including function_name) is already satisfied -- the docstring at parse_payload.py:99 already lists function_name. No action needed.

Purpose: Dependency hygiene and export cleanliness -- each package declares only what it imports, and public APIs expose only what is consumed.
Output: Five corrected files, all tests passing, uv lock file updated.
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tech-debt-cleanup-r2/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dependency declarations across workspace packages</name>
  <files>
    backend/pyproject.toml
    utils/pyproject.toml
    pyproject.toml
  </files>
  <action>
    Three dependency fixes, all in pyproject.toml files:

    1. **Remove tenacity from backend** (`backend/pyproject.toml`):
       Delete the line `"tenacity>=8.3",` from the `dependencies` list. No file in `backend/src/` imports tenacity.

    2. **Move PyYAML from utils to backend** (MUST be atomic -- both edits together):
       - In `utils/pyproject.toml`: Remove `"PyYAML>=6.0.1",` from the `dependencies` list.
       - In `backend/pyproject.toml`: Add `"PyYAML>=6.0.1",` to the `dependencies` list (after `pydantic-settings` line, alphabetically near other P-entries).
       - Rationale: `backend/src/ferry_backend/config/loader.py` does `import yaml`, not `utils/`.

    3. **Add stepfunctions to moto extras** (`pyproject.toml` root):
       Change `"moto[dynamodb,apigateway]>=5.0",` to `"moto[dynamodb,apigateway,stepfunctions]>=5.0",`.

    After all three edits, run:
    ```
    uv lock && uv sync --all-packages
    ```
    Then verify the import chain works:
    ```
    python -c "from ferry_backend.config.loader import parse_config; print('PyYAML import OK')"
    ```
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && uv lock && uv sync --all-packages && python -c "from ferry_backend.config.loader import parse_config; print('OK')" && uv run pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
    <manual>Confirm backend/pyproject.toml has PyYAML and no tenacity, utils/pyproject.toml has no PyYAML, root pyproject.toml has moto stepfunctions extra</manual>
  </verify>
  <done>
    - tenacity>=8.3 is not present in backend/pyproject.toml
    - PyYAML>=6.0.1 is present in backend/pyproject.toml dependencies
    - PyYAML is not present in utils/pyproject.toml dependencies
    - moto extras in root pyproject.toml include stepfunctions
    - uv lock succeeds and uv sync --all-packages installs cleanly
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove PushEvent and WebhookHeaders from public exports</name>
  <files>
    utils/src/ferry_utils/__init__.py
    utils/src/ferry_utils/models/__init__.py
  </files>
  <action>
    Remove unused models from public re-exports. Keep the model definitions and tests intact.

    1. **In `utils/src/ferry_utils/models/__init__.py`:**
       - Remove the entire import block for webhook models:
         ```python
         from ferry_utils.models.webhook import (
             Pusher,
             PushEvent,
             Repository,
             WebhookHeaders,
         )
         ```
       - Remove `"PushEvent"`, `"Pusher"`, `"Repository"`, and `"WebhookHeaders"` from `__all__`.
       - Note: The success criteria only calls out PushEvent and WebhookHeaders, but Pusher and Repository are also unused in production code (confirmed by grep in research). Clean all four webhook re-exports from this file for consistency.

    2. **In `utils/src/ferry_utils/__init__.py`:**
       - Remove the import line: `from ferry_utils.models.webhook import PushEvent, WebhookHeaders`
       - Remove `"PushEvent"` and `"WebhookHeaders"` from `__all__`.

    DO NOT touch:
    - `utils/src/ferry_utils/models/webhook.py` (keep model definitions)
    - `tests/test_webhook_models.py` (keep tests -- they import directly from webhook module)
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && python -c "from ferry_utils import __all__; assert 'PushEvent' not in __all__; assert 'WebhookHeaders' not in __all__; print('Exports clean')" && uv run pytest tests/ -x -q --tb=short 2>&1 | tail -5</automated>
    <manual>Confirm PushEvent and WebhookHeaders are not in __all__ of either __init__.py file</manual>
  </verify>
  <done>
    - PushEvent not in ferry_utils.__all__ or ferry_utils.models.__all__
    - WebhookHeaders not in ferry_utils.__all__ or ferry_utils.models.__all__
    - webhook.py model definitions are unchanged
    - test_webhook_models.py still passes (imports directly from webhook module)
    - All existing tests pass
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
cd /Users/amit/Repos/github/ferry && uv run pytest tests/ -x -q --tb=short
```

Verify dependency correctness:
```bash
# tenacity gone from backend
grep -c "tenacity" backend/pyproject.toml  # should be 0
# PyYAML in backend, not in utils
grep -c "PyYAML" backend/pyproject.toml    # should be 1
grep -c "PyYAML" utils/pyproject.toml      # should be 0
# stepfunctions in moto extras
grep "moto" pyproject.toml                 # should show stepfunctions
# No PushEvent/WebhookHeaders in exports
python -c "from ferry_utils import __all__; print(__all__)"  # should not contain PushEvent or WebhookHeaders
```
</verification>

<success_criteria>
1. `backend/pyproject.toml` declares PyYAML>=6.0.1, does NOT declare tenacity
2. `utils/pyproject.toml` does NOT declare PyYAML
3. Root `pyproject.toml` has `moto[dynamodb,apigateway,stepfunctions]>=5.0`
4. `PushEvent` and `WebhookHeaders` are not in `__all__` of `ferry_utils` or `ferry_utils.models`
5. `uv lock && uv sync --all-packages` succeeds
6. `uv run pytest tests/ -x` passes all tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-tech-debt-cleanup-r2/09-01-SUMMARY.md`
</output>
