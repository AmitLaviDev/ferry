---
phase: 07-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/phases/03-build-and-lambda-deploy/03-03-SUMMARY.md
  - action/src/ferry_action/parse_payload.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "03-03-SUMMARY no longer claims DEPLOY-01 (only 06-01 claims it, matching REQUIREMENTS.md traceability)"
    - "No stale comments or docstrings remain that contradict the current runtime wiring"
    - "Any trivial inconsistencies found in the codebase sweep are fixed"
  artifacts:
    - path: ".planning/phases/03-build-and-lambda-deploy/03-03-SUMMARY.md"
      provides: "Corrected requirements-completed removing DEPLOY-01"
      contains: "requirements-completed: [DEPLOY-04, DEPLOY-05]"
  key_links: []
---

<objective>
Fix the SUMMARY frontmatter mismatch for DEPLOY-01 and perform a codebase-wide sweep for trivial inconsistencies remaining after the runtime wiring.

Purpose: Ensure the planning metadata accurately reflects which phase completed each requirement (03-03 partially implemented deploy but DEPLOY-01 was closed by Phase 6). Clean up any remaining stale comments or trivial issues.

Output: Corrected 03-03-SUMMARY.md, clean codebase with no stale references to old runtime defaults or incorrect pipeline documentation.
</objective>

<execution_context>
@/Users/amit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-cleanup/07-RESEARCH.md
@.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 03-03-SUMMARY DEPLOY-01 frontmatter and cross-validate all SUMMARYs</name>
  <files>
    .planning/phases/03-build-and-lambda-deploy/03-03-SUMMARY.md
  </files>
  <action>
    1. **Fix 03-03-SUMMARY.md**: Change `requirements-completed: [DEPLOY-01, DEPLOY-04, DEPLOY-05]` to `requirements-completed: [DEPLOY-04, DEPLOY-05]`. Rationale: Phase 3 created deploy.py but the function_name pipeline was broken -- DEPLOY-01 was not fully satisfied until Phase 6 fixed it. The REQUIREMENTS.md traceability table maps DEPLOY-01 to Phase 6, not Phase 3. Only 06-01-SUMMARY should claim DEPLOY-01.

    2. **Cross-validate all 13 SUMMARYs** against REQUIREMENTS.md traceability table. Read each SUMMARY's `requirements-completed` and compare against the phase assignment in REQUIREMENTS.md. The research already verified all are correct except the 03-03 DEPLOY-01 issue. Confirm no other mismatches exist after the fix.

    The expected final mapping:
    - 01-01: [ACT-02] -- correct (ACT-02 -> Phase 1)
    - 01-02: [WHOOK-01, WHOOK-02] -- correct (both -> Phase 1)
    - 01-03: [AUTH-01] -- correct (AUTH-01 -> Phase 1)
    - 02-01: [CONF-01, CONF-02] -- correct (both -> Phase 2)
    - 02-02: [DETECT-01] -- correct (DETECT-01 -> Phase 2)
    - 02-03: [DETECT-02, ORCH-01, ORCH-02] -- correct (all -> Phase 2)
    - 03-01: [ACT-01, AUTH-02] -- correct (both -> Phase 3)
    - 03-02: [BUILD-01, BUILD-02, BUILD-03, BUILD-04, BUILD-05] -- correct (all -> Phase 3)
    - 03-03: [DEPLOY-04, DEPLOY-05] -- FIXED (was [DEPLOY-01, DEPLOY-04, DEPLOY-05])
    - 04-01: [DEPLOY-02, DEPLOY-03] -- correct (both -> Phase 4)
    - 04-02: [DEPLOY-02] -- correct (DEPLOY-02 -> Phase 4)
    - 04-03: [DEPLOY-03] -- correct (DEPLOY-03 -> Phase 4)
    - 06-01: [DEPLOY-01] -- correct (DEPLOY-01 -> Phase 6)
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && grep "requirements-completed" .planning/phases/03-build-and-lambda-deploy/03-03-SUMMARY.md | grep -v "DEPLOY-01" && echo "OK: DEPLOY-01 removed from 03-03"</automated>
  </verify>
  <done>03-03-SUMMARY requirements-completed is [DEPLOY-04, DEPLOY-05] (DEPLOY-01 removed). All 13 SUMMARYs cross-validated against REQUIREMENTS.md -- no mismatches remain.</done>
</task>

<task type="auto">
  <name>Task 2: Codebase sweep for stale comments and trivial inconsistencies</name>
  <files>
    action/src/ferry_action/parse_payload.py
  </files>
  <action>
    Perform a codebase-wide sweep for trivial inconsistencies. The research identified specific items; fix those and search for others.

    **Known issues to verify/fix (from 07-RESEARCH.md):**

    1. **parse_payload.py `_build_lambda_matrix` docstring** -- After Plan 01 rewrites this docstring, verify the new version is accurate. If Plan 01 left any inaccuracy, fix it. The docstring should mention that runtime comes from the dispatch payload (source of truth: LambdaConfig), not hardcoded.

    2. **parse_payload.py `build_matrix` docstring (around line 101)** -- Verify the lambda field list includes `function_name` (was missing per Phase 6 verification). If Plan 01 already fixed this, confirm. If not, add `function_name` to the field list.

    **Sweep checklist (search and verify):**

    3. Search ALL production Python files for `python3.10` and `python3.12` -- after Plan 01, these should only appear in test files as explicit override values (not defaults). If any remain in source files, fix them.

    4. Search for stale `# TODO` or `# FIXME` or `# HACK` comments in production code. Flag non-trivial ones, fix trivial ones.

    5. Search for `# type: ignore` annotations -- the research confirmed these are all intentional (boto3 typing, Pydantic testing). Verify no new unintentional ones crept in.

    6. Verify docstrings on key functions match their current behavior:
       - `_build_resource` in trigger.py -- should mention runtime in the docstring
       - `build_matrix` in parse_payload.py -- field lists should be accurate
       - `deploy_lambda` or `main` in deploy.py -- should mention function_name

    7. Check that all `__init__.py` re-exports are up to date (LambdaResource now has runtime, but re-exports are at the class level so this should be fine -- just verify).

    For anything non-trivial found during the sweep, document it as a note in the SUMMARY rather than fixing it in this task.
  </action>
  <verify>
    <automated>cd /Users/amit/Repos/github/ferry && grep -rn "python3.10\|python3.12" backend/src/ utils/src/ action/src/ 2>/dev/null; echo "---"; uv run pytest tests/ -x -q 2>&1 | tail -3</automated>
    <manual>Verify no stale runtime references remain in production source files. Any hits should only be in test files.</manual>
  </verify>
  <done>No stale runtime defaults in production source files. All docstrings accurate. No trivial inconsistencies remain. Any non-trivial findings documented in SUMMARY.</done>
</task>

</tasks>

<verification>
1. `grep "requirements-completed" .planning/phases/03-build-and-lambda-deploy/03-03-SUMMARY.md` shows `[DEPLOY-04, DEPLOY-05]` (no DEPLOY-01)
2. `grep -rn "python3.10\|python3.12" backend/src/ utils/src/ action/src/` returns no hits
3. `uv run pytest tests/ -x -q` -- all tests pass
4. parse_payload.py docstrings mention function_name and runtime correctly
</verification>

<success_criteria>
- 03-03-SUMMARY no longer claims DEPLOY-01
- All 13 SUMMARYs cross-validated against REQUIREMENTS.md
- No stale runtime defaults in production code
- All docstrings accurate for current code behavior
- All tests still pass after any sweep fixes
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-03-SUMMARY.md`
</output>
